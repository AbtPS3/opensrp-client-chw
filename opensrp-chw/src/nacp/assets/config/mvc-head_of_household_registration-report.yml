indicators:
  - key: "mvc-child-headed-M-17"
    grouping: "MVC"
    description: "Number of Households Headed by Male Children aged 17 and below"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob))  <=17
                      AND head_of_household = 'child'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-child-headed-F-17"
    grouping: "MVC"
    description: "Number of Households Headed by Fema Children aged 17 and below"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob))  <=17
                      AND head_of_household = 'child'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-adult-headed-M-17"
    grouping: "MVC"
    description: "Number of Households Headed by Male Adult aged 17 and below"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob))  <=17
                      AND head_of_household = 'adult'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-adult-headed-F-17"
    grouping: "MVC"
    description: "Number of Households Headed by Female Adult aged 17 and below"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob))  <=17
                      AND head_of_household = 'adult'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-elderly-headed-M-17"
    grouping: "MVC"
    description: "Number of Households Headed by Male Adult aged 17 and below"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob))  <=17
                      AND head_of_household = 'elderly'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-elderly-headed-F-17"
    grouping: "MVC"
    description: "Number of Households Headed by Female Adult aged 17 and below"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob))  <=17
                      AND head_of_household = 'elderly'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-employed-M-17"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 17 and below who are employed"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob))  <=17
					  AND efm.service_provider IS NOT NULL AND efm.service_provider = 'chk_self_employed' 
					  OR efm.service_provider ='chk_employed' 
					  OR efm.service_provider ='chk_farmer' 
					  OR efm.service_provider='chk_traditional_healer'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-employed-F-17"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 17 and below who are employed"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob))  <=17
					  AND efm.service_provider IS NOT NULL AND efm.service_provider = 'chk_self_employed' 
					  OR efm.service_provider ='chk_employed' 
					  OR efm.service_provider ='chk_farmer' 
					  OR efm.service_provider='chk_traditional_healer'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-unemployed-M-17"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 17 and below who are unemployed"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob))  <=17
					  AND efm.service_provider IS NOT NULL AND efm.service_provider = 'chk_unemployed' 
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-unemployed-F-17"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 17 and below and who are unemployed"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob))  <=17
					  AND efm.service_provider IS NOT NULL AND efm.service_provider = 'chk_unemployed' 
                      AND (efm.gender='Female')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-insured-M-17"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 17 and below who are insured"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                    FROM ec_ovc_register eor
                    INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                    WHERE  (date('Now') - date(efm.dob))  <=17
                    AND efm.insurance_provider != 'No_Health_Insurance'
                    AND (efm.gender='Male')
                    AND eor.client_type = 'family_head'
                    AND date((substr('%s', 1, 7) || '-' || '01')) =
                    date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-insured-F-17"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 17 and below who are insured"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                    FROM ec_ovc_register eor
                    INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                    WHERE  (date('Now') - date(efm.dob))  <=17
                    AND efm.insurance_provider != 'No_Health_Insurance'
                    AND (efm.gender='Female')
                    AND eor.client_type = 'family_head'
                    AND date((substr('%s', 1, 7) || '-' || '01')) =
                    date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-uninsured-M-17"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 17 and below who are not insured"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob))  <=17
					  AND efm.insurance_provider <> 'No_Health_Insurance'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-uninsured-F-17"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 17 and below who are not insured"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob))  <=17
					  AND efm.insurance_provider <> 'No_Health_Insurance'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-disabled-M-17"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 17 and below and are disabled"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob))  <=17
					  AND eor.type_of_disability IS NOT NULL
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-disabled-F-17"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 17 and below and are disabled"
    indicatorQuery: "  SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob))  <=17
					  AND eor.type_of_disability IS NOT NULL
                      AND (efm.gender='Female')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-hiv-M-17"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 17 and below and have hiv"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob))  <=17
					  AND eor.type_of_vulnerability ='living_with_hiv'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-hiv-F-17"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 17 and below and have hiv"
    indicatorQuery: "SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob))  <=17
					  AND eor.type_of_vulnerability ='living_with_hiv'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-chronic-M-17"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 17 and below and have chronic vulnerabilities"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob))  <=17
					  AND eor.type_of_vulnerability ='chronic_disease'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-chronic-F-17"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 17 and below and have chronic vulnerabilities"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob))  <=17
					  AND eor.type_of_vulnerability ='chronic_disease'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

#    Queries for the age group 18-24 starts here

  - key: "mvc-child-headed-M-18-24"
    grouping: "MVC"
    description: "Number of Households Headed by Male Children aged 18-24"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 17 AND (date('Now') - date(efm.dob)) < 25
                      AND head_of_household = 'child'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-child-headed-F-18-24"
    grouping: "MVC"
    description: "Number of Households Headed by Fema Children aged 18-24"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 17 AND (date('Now') - date(efm.dob)) < 25
                      AND head_of_household = 'child'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-adult-headed-M-18-24"
    grouping: "MVC"
    description: "Number of Households Headed by Male Adult aged 18-24"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 17 AND (date('Now') - date(efm.dob)) < 25
                      AND head_of_household = 'adult'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-adult-headed-F-18-24"
    grouping: "MVC"
    description: "Number of Households Headed by Female Adult aged 18-24"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 17 AND (date('Now') - date(efm.dob)) < 25
                      AND head_of_household = 'adult'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-elderly-headed-M-18-24"
    grouping: "MVC"
    description: "Number of Households Headed by Male Adult aged 18-24"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 17 AND (date('Now') - date(efm.dob)) < 25
                      AND head_of_household = 'elderly'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-elderly-headed-F-18-24"
    grouping: "MVC"
    description: "Number of Households Headed by Female Adult aged 18-24"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 17 AND (date('Now') - date(efm.dob)) < 25
                      AND head_of_household = 'elderly'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-employed-M-18-24"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 18-24 who are employed"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 17 AND (date('Now') - date(efm.dob)) < 25
					  AND efm.service_provider IS NOT NULL AND efm.service_provider = 'chk_self_employed' 
					  OR efm.service_provider ='chk_employed' 
					  OR efm.service_provider ='chk_farmer' 
					  OR efm.service_provider='chk_traditional_healer'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-employed-F-18-24"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 18-24 who are employed"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 17 AND (date('Now') - date(efm.dob)) < 25
					  AND efm.service_provider IS NOT NULL AND efm.service_provider = 'chk_self_employed' 
					  OR efm.service_provider ='chk_employed' 
					  OR efm.service_provider ='chk_farmer' 
					  OR efm.service_provider='chk_traditional_healer'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-unemployed-M-18-24"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 18-24 who are unemployed"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 17 AND (date('Now') - date(efm.dob)) < 25
					  AND efm.service_provider IS NOT NULL AND efm.service_provider = 'chk_unemployed' 
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-unemployed-F-18-24"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 18-24 and who are unemployed"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 17 AND (date('Now') - date(efm.dob)) < 25
					  AND efm.service_provider IS NOT NULL AND efm.service_provider = 'chk_unemployed' 
                      AND (efm.gender='Female')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-insured-M-18-24"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 18-24 who are insured"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                    FROM ec_ovc_register eor
                    INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                    WHERE  (date('Now') - date(efm.dob)) > 17 AND (date('Now') - date(efm.dob)) < 25
                    AND efm.insurance_provider != 'No_Health_Insurance'
                    AND (efm.gender='Male')
                    AND eor.client_type = 'family_head'
                    AND date((substr('%s', 1, 7) || '-' || '01')) =
                    date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-insured-F-18-24"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 18-24 who are insured"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                    FROM ec_ovc_register eor
                    INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                    WHERE  (date('Now') - date(efm.dob)) > 17 AND (date('Now') - date(efm.dob)) < 25
                    AND efm.insurance_provider != 'No_Health_Insurance'
                    AND (efm.gender='Female')
                    AND eor.client_type = 'family_head'
                    AND date((substr('%s', 1, 7) || '-' || '01')) =
                    date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-uninsured-M-18-24"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 18-24 who are not insured"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 17 AND (date('Now') - date(efm.dob)) < 25
					  AND efm.insurance_provider <> 'No_Health_Insurance'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-uninsured-F-18-24"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 18-24 who are not insured"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 17 AND (date('Now') - date(efm.dob)) < 25
					  AND efm.insurance_provider <> 'No_Health_Insurance'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-disabled-M-18-24"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 18-24 and are disabled"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 17 AND (date('Now') - date(efm.dob)) < 25
					  AND eor.type_of_disability IS NOT NULL
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-disabled-F-18-24"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 18-24 and are disabled"
    indicatorQuery: "  SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 17 AND (date('Now') - date(efm.dob)) < 25
					  AND eor.type_of_disability IS NOT NULL
                      AND (efm.gender='Female')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-hiv-M-18-24"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 18-24 and have hiv"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 17 AND (date('Now') - date(efm.dob)) < 25
					  AND eor.type_of_vulnerability ='living_with_hiv'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-hiv-F-18-24"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 18-24 and have hiv"
    indicatorQuery: "SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 17 AND (date('Now') - date(efm.dob)) < 25
					  AND eor.type_of_vulnerability ='living_with_hiv'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-chronic-M-18-24"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 18-24 and have chronic vulnerabilities"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 17 AND (date('Now') - date(efm.dob)) < 25
					  AND eor.type_of_vulnerability ='chronic_disease'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-chronic-F-18-24"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 18-24 and have chronic vulnerabilities"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 17 AND (date('Now') - date(efm.dob)) < 25
					  AND eor.type_of_vulnerability ='chronic_disease'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"



  #    Queries for the age group 25-59 starts here

  - key: "mvc-child-headed-M-25-59"
    grouping: "MVC"
    description: "Number of Households Headed by Male Children aged 25-59"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 25 AND (date('Now') - date(efm.dob)) < 60
                      AND head_of_household = 'child'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-child-headed-F-25-59"
    grouping: "MVC"
    description: "Number of Households Headed by Fema Children aged 18-24"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 25 AND (date('Now') - date(efm.dob)) < 60
                      AND head_of_household = 'child'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-adult-headed-M-25-59"
    grouping: "MVC"
    description: "Number of Households Headed by Male Adult aged 25-59"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 25 AND (date('Now') - date(efm.dob)) < 60
                      AND head_of_household = 'adult'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-adult-headed-F-25-59"
    grouping: "MVC"
    description: "Number of Households Headed by Female Adult aged 25-59"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 25 AND (date('Now') - date(efm.dob)) < 60
                      AND head_of_household = 'adult'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-elderly-headed-M-25-59"
    grouping: "MVC"
    description: "Number of Households Headed by Male Adult aged 25-59"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 25 AND (date('Now') - date(efm.dob)) < 60
                      AND head_of_household = 'elderly'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-elderly-headed-F-25-59"
    grouping: "MVC"
    description: "Number of Households Headed by Female Adult aged 25-59"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 25 AND (date('Now') - date(efm.dob)) < 60
                      AND head_of_household = 'elderly'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-employed-M-25-59"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 25-59 who are employed"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 25 AND (date('Now') - date(efm.dob)) < 60
					  AND efm.service_provider IS NOT NULL AND efm.service_provider = 'chk_self_employed' 
					  OR efm.service_provider ='chk_employed' 
					  OR efm.service_provider ='chk_farmer' 
					  OR efm.service_provider='chk_traditional_healer'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-employed-F-25-59"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 25-59 who are employed"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 25 AND (date('Now') - date(efm.dob)) < 60
					  AND efm.service_provider IS NOT NULL AND efm.service_provider = 'chk_self_employed' 
					  OR efm.service_provider ='chk_employed' 
					  OR efm.service_provider ='chk_farmer' 
					  OR efm.service_provider='chk_traditional_healer'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-unemployed-M-25-59"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 25-59 who are unemployed"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 25 AND (date('Now') - date(efm.dob)) < 60
					  AND efm.service_provider IS NOT NULL AND efm.service_provider = 'chk_unemployed' 
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-unemployed-F-25-59"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 25-59 and who are unemployed"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 25 AND (date('Now') - date(efm.dob)) < 60
					  AND efm.service_provider IS NOT NULL AND efm.service_provider = 'chk_unemployed' 
                      AND (efm.gender='Female')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-insured-M-25-59"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 25-59 who are insured"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                    FROM ec_ovc_register eor
                    INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                    WHERE  (date('Now') - date(efm.dob)) >= 25 AND (date('Now') - date(efm.dob)) < 60
                    AND efm.insurance_provider != 'No_Health_Insurance'
                    AND (efm.gender='Male')
                    AND eor.client_type = 'family_head'
                    AND date((substr('%s', 1, 7) || '-' || '01')) =
                    date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-insured-F-25-59"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 25-59 who are insured"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                    FROM ec_ovc_register eor
                    INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                    WHERE  (date('Now') - date(efm.dob)) >= 25 AND (date('Now') - date(efm.dob)) < 60
                    AND efm.insurance_provider != 'No_Health_Insurance'
                    AND (efm.gender='Female')
                    AND eor.client_type = 'family_head'
                    AND date((substr('%s', 1, 7) || '-' || '01')) =
                    date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-uninsured-M-25-59"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 25-59 who are not insured"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 25 AND (date('Now') - date(efm.dob)) < 60
					  AND efm.insurance_provider <> 'No_Health_Insurance'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-uninsured-F-25-59"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 25-59 who are not insured"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 25 AND (date('Now') - date(efm.dob)) < 60
					  AND efm.insurance_provider <> 'No_Health_Insurance'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-disabled-M-25-59"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 25-59 and are disabled"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 25 AND (date('Now') - date(efm.dob)) < 60
					  AND eor.type_of_disability IS NOT NULL
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-disabled-F-25-59"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 25-59 and are disabled"
    indicatorQuery: "  SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 25 AND (date('Now') - date(efm.dob)) < 60
					  AND eor.type_of_disability IS NOT NULL
                      AND (efm.gender='Female')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-hiv-M-25-59"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 25-59 and have hiv"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 25 AND (date('Now') - date(efm.dob)) < 60
					  AND eor.type_of_vulnerability ='living_with_hiv'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-hiv-F-25-59"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 25-59 and have hiv"
    indicatorQuery: "SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 25 AND (date('Now') - date(efm.dob)) < 60
					  AND eor.type_of_vulnerability ='living_with_hiv'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-chronic-M-25-59"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 25-59 and have chronic vulnerabilities"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 25 AND (date('Now') - date(efm.dob)) < 60
					  AND eor.type_of_vulnerability ='chronic_disease'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-chronic-F-25-59"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 25-59 and have chronic vulnerabilities"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 25 AND (date('Now') - date(efm.dob)) < 60
					  AND eor.type_of_vulnerability ='chronic_disease'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"




  #    Queries for the age group 60+ starts here

  - key: "mvc-child-headed-M-60-and-above"
    grouping: "MVC"
    description: "Number of Households Headed by Male Children aged 60-and-above"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 60
                      AND head_of_household = 'child'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-child-headed-F-60-and-above"
    grouping: "MVC"
    description: "Number of Households Headed by Fema Children aged 18-24"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 60
                      AND head_of_household = 'child'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-adult-headed-M-60-and-above"
    grouping: "MVC"
    description: "Number of Households Headed by Male Adult aged 60-and-above"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 60
                      AND head_of_household = 'adult'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-adult-headed-F-60-and-above"
    grouping: "MVC"
    description: "Number of Households Headed by Female Adult aged 60-and-above"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 60
                      AND head_of_household = 'adult'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-elderly-headed-M-60-and-above"
    grouping: "MVC"
    description: "Number of Households Headed by Male Adult aged 60-and-above"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 60
                      AND head_of_household = 'elderly'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-elderly-headed-F-60-and-above"
    grouping: "MVC"
    description: "Number of Households Headed by Female Adult aged 60-and-above"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 60
                      AND head_of_household = 'elderly'
                      AND eor.client_type = 'family_head'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                          date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-employed-M-60-and-above"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 60-and-above who are employed"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 60
					  AND efm.service_provider IS NOT NULL AND efm.service_provider = 'chk_self_employed' 
					  OR efm.service_provider ='chk_employed' 
					  OR efm.service_provider ='chk_farmer' 
					  OR efm.service_provider='chk_traditional_healer'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-employed-F-60-and-above"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 60-and-above who are employed"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 60
					  AND efm.service_provider IS NOT NULL AND efm.service_provider = 'chk_self_employed' 
					  OR efm.service_provider ='chk_employed' 
					  OR efm.service_provider ='chk_farmer' 
					  OR efm.service_provider='chk_traditional_healer'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-unemployed-M-60-and-above"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 60-and-above who are unemployed"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 60
					  AND efm.service_provider IS NOT NULL AND efm.service_provider = 'chk_unemployed' 
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-unemployed-F-60-and-above"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 60-and-above and who are unemployed"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 60
					  AND efm.service_provider IS NOT NULL AND efm.service_provider = 'chk_unemployed' 
                      AND (efm.gender='Female')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-insured-M-60-and-above"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 60-and-above who are insured"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                    FROM ec_ovc_register eor
                    INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 60
                    AND efm.insurance_provider != 'No_Health_Insurance'
                    AND (efm.gender='Male')
                    AND eor.client_type = 'family_head'
                    AND date((substr('%s', 1, 7) || '-' || '01')) =
                    date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-insured-F-60-and-above"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 60-and-above who are insured"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                    FROM ec_ovc_register eor
                    INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                    WHERE  (date('Now') - date(efm.dob)) >= 60
                    AND efm.insurance_provider != 'No_Health_Insurance'
                    AND (efm.gender='Female')
                    AND eor.client_type = 'family_head'
                    AND date((substr('%s', 1, 7) || '-' || '01')) =
                    date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-uninsured-M-60-and-above"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 60-and-above who are not insured"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 60
					  AND efm.insurance_provider <> 'No_Health_Insurance'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-uninsured-F-60-and-above"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 60-and-above who are not insured"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 60
					  AND efm.insurance_provider <> 'No_Health_Insurance'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-disabled-M-60-and-above"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 60-and-above and are disabled"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 60
					  AND eor.type_of_disability IS NOT NULL
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-disabled-F-60-and-above"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 60-and-above and are disabled"
    indicatorQuery: "  SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 60
					  AND eor.type_of_disability IS NOT NULL
                      AND (efm.gender='Female')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-hiv-M-60-and-above"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 60-and-above and have hiv"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 60
					  AND eor.type_of_vulnerability ='living_with_hiv'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-hiv-F-60-and-above"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 60-and-above and have hiv"
    indicatorQuery: "SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 60
					  AND eor.type_of_vulnerability ='living_with_hiv'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-chronic-M-60-and-above"
    grouping: "MVC"
    description: "Number of Male primary primary caregivers aged 60-and-above and have chronic vulnerabilities"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 60
					  AND eor.type_of_vulnerability ='chronic_disease'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  - key: "mvc-chronic-F-60-and-above"
    grouping: "MVC"
    description: "Number of Female primary primary caregivers aged 60-and-above and have chronic vulnerabilities"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 60
					  AND eor.type_of_vulnerability ='chronic_disease'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'family_head'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"