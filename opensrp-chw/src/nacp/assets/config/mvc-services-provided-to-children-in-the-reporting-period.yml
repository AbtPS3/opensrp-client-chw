#Indicators for less than 1
indicators:
  #Male
  - key: "mvc-number-of-children-enrolled-in-school-M-<1"
    grouping: "MVC"
    description: "Number male children registered in school aged under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.enrollment_date / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-number-of-children-enrolled-in-school-F-<1"
    grouping: "MVC"
    description: "Number Female children registered in school aged under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.enrollment_date / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-missed-school-M-<1"
    grouping: "MVC"
    description: "Number of Male children who missed school for three or more days consecutively in a month aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.education_and_vocational_training = 'missed_school_three_days'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-missed-school-F-<1"
    grouping: "MVC"
    description: "Number of Female children who missed school for three or more days consecutively in a month aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.education_and_vocational_training = 'missed_school_three_days'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-linked-to-education-support-M-<1"
    grouping: "MVC"
    description: "Number of Male children who are linked to education and support and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.education_vocational_training like '%linked_to_education_support%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-linked-to-education-support-F-<1"
    grouping: "MVC"
    description: "Number of Female children who are linked to education and support and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.education_vocational_training like '%linked_to_education_support%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-supported-vocational-training-M-<1"
    grouping: "MVC"
    description: "Number of Male children who are linked to vocational training support and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.ecd_psychosocial_support like '%supported_attend_vocational_training%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-supported-vocational-training-F-<1"
    grouping: "MVC"
    description: "Number of Female children who are linked to vocational trining support and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.ecd_psychosocial_support like '%supported_attend_vocational_training%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"



  #Male
  - key: "mvc-received-business-kit-M-<1"
    grouping: "MVC"
    description: "Number of Male children who received business kit and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.ecd_psychosocial_support like '%received_business_start_up_kit%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-received-business-kit-F-<1"
    grouping: "MVC"
    description: "Number of Female children who received business kit and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.ecd_psychosocial_support like '%received_business_start_up_kit%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-linked-to-memkwa-M-<1"
    grouping: "MVC"
    description: "Number of Male children who received business kit and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.ecd_psychosocial_support like '%linked_mainstreamed_education_program%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-to-memkwa-F-<1"
    grouping: "MVC"
    description: "Number of Female children who received business kit and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.ecd_psychosocial_support like '%linked_mainstreamed_education_program%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

###############################################Education and vocational training ends here ###########################################################################

###############################################ECD & Psychosocial Support starts here ###########################################################################

  #Male
  - key: "mvc-attended-ecd-M-<1"
    grouping: "MVC"
    description: "Number of Male children who attended ECD and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.ecd_psychosocial_support like '%attended_ecd_centre%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-attended-ecd-F-<1"
    grouping: "MVC"
    description: "Number of Female children who received business kit and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.ecd_psychosocial_support like '%attended_ecd_centre%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-attended-childrens-clubs-M-<1"
    grouping: "MVC"
    description: "Number of Male children attended children's clubs and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.ecd_psychosocial_support like '%children_attended_childrens_clubs%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-attended-childrens-clubs-F-<1"
    grouping: "MVC"
    description: "Number of Female children attended children's clubs and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.ecd_psychosocial_support like '%children_attended_childrens_clubs%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-attended-teen-clubs-M-<1"
    grouping: "MVC"
    description: "Number of Male children attended teen's clubs and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.ecd_psychosocial_support like '%children_attended_teen_clubs%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-attended-teen-clubs-F-<1"
    grouping: "MVC"
    description: "Number of Female children attended teen's clubs and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.ecd_psychosocial_support like '%children_attended_teen_clubs%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

############################################### MVC<5 years Vaccination  ###########################################################################

  #Male
  - key: "mvc-vaccinated-M-<1"
    grouping: "MVC"
    description: "Number of Male MVC < 5 years received required Vaccination and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.child_received_vaccinations = 'yes'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-vaccinated-F-<1"
    grouping: "MVC"
    description: "Number of Female MVC < 5 years received required Vaccination and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.child_received_vaccinations = 'yes'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-not-vaccinated-M-<1"
    grouping: "MVC"
    description: "Number of Male MVC < 5 years who did not receive required Vaccination and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.child_received_vaccinations = 'no'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-not-vaccinated-F-<1"
    grouping: "MVC"
    description: "Number of Female MVC < 5 years who did not receive required Vaccination and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.child_received_vaccinations = 'no'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

############################################### Nutritional Status  ###########################################################################

  #Male
  - key: "mvc-severe-malnutrition-M-<1"
    grouping: "MVC"
    description: "Number of Male MVC with Severe Malnutrition and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.child_nutritional_status = 'severe_malnutrition'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-severe-malnutrition-F-<1"
    grouping: "MVC"
    description: "Number of Female MVC with Severe Malnutrition and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.child_nutritional_status = 'severe_malnutrition'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-moderate-malnutrition-M-<1"
    grouping: "MVC"
    description: "Number of Male MVC with Severe Malnutrition and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.child_nutritional_status = 'moderate_malnutrition'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-moderate-malnutrition-M-<1"
    grouping: "MVC"
    description: "Number of Female MVC with Severe Malnutrition and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.child_nutritional_status = 'moderate_malnutrition'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  ############################################### Health Care  ###########################################################################

  #Male
  - key: "mvc-joined-chf-tika-M-<1"
    grouping: "MVC"
    description: "Number of Male Children supported to join CHF/TIKA and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.health_care_service_provided like '%supported_to_join_chf_tika%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-joined-chf-tika-F-<1"
    grouping: "MVC"
    description: "Number of Female Children supported to join CHF/TIKA and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.health_care_service_provided like '%supported_to_join_chf_tika%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-hiv-prevention-counseling-M-<1"
    grouping: "MVC"
    description: "Number of Male children received HIV prevention counseling services and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.health_care_service_provided like '%hiv_prevention_counseling_service%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-hiv-prevention-counseling-F-<1"
    grouping: "MVC"
    description: "Number of Female children received HIV prevention counseling services and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.health_care_service_provided like '%hiv_prevention_counseling_service%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-disclosure-support-M-<1"
    grouping: "MVC"
    description: "Number of Male Children received disclosure support and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.health_care_service_provided like '%disclosure_support%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-disclosure-support-F-<1"
    grouping: "MVC"
    description: "Number of Female Children received disclosure support and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.health_care_service_provided like '%disclosure_support%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"



  #Male
  - key: "mvc-linked-hiv-support-group-M-<1"
    grouping: "MVC"
    description: "Number of Male Children linked to HIV support group and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.health_care_service_provided like '%hiv_support_group%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-hiv-support-group-F-<1"
    grouping: "MVC"
    description: "Number of Female Children linked to HIV support group and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.health_care_service_provided like '%hiv_support_group%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"





  #Male
  - key: "mvc-art-adherence-counseling-M-<1"
    grouping: "MVC"
    description: "Number of Male Children received ART adherence counseling and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.health_care_service_provided like '%art_adherence_counseling%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-art-adherence-counseling-F-<1"
    grouping: "MVC"
    description: "Number of Female Children received ART adherence counseling and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.health_care_service_provided like '%art_adherence_counseling%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-hiv-risk-assessment-M-<1"
    grouping: "MVC"
    description: "Number of Male children received HIV risk assessment and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.health_care_service_provided like '%hiv_risk_assessment%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-hiv-risk-assessment-F-<1"
    grouping: "MVC"
    description: "Number of Female children received HIV risk assessment and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.health_care_service_provided like '%hiv_risk_assessment%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-hiv-prevention-education-M-<1"
    grouping: "MVC"
    description: "Number of Male Children provided with HIV prevention education and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.health_care_service_provided like '%hiv_prevention_education%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-hiv-prevention-education-F-<1"
    grouping: "MVC"
    description: "Number of Female Children provided with HIV prevention education and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.health_care_service_provided like '%hiv_prevention_education%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-supported-birth-certificates-M-<1"
    grouping: "MVC"
    description: "Number of Male Children supported to get birth certificates and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.child_protection_service like '%supported_to_get_birth_certificates%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-supported-birth-certificates-F-<1"
    grouping: "MVC"
    description: "Number of Female Children supported to get birth certificates and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.child_protection_service like '%supported_to_get_birth_certificates%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-emergency-care-M-<1"
    grouping: "MVC"
    description: "Number of Male Children supported to get emergency care and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.child_protection_service like '%provided_with_emergency_care_and_support_services%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-emergency-care-F-<1"
    grouping: "MVC"
    description: "Number of Female Children supported to get emergency care and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.child_protection_service like '%provided_with_emergency_care_and_support_services%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-linked-social-welfare-M-<1"
    grouping: "MVC"
    description: "Number of Male Children linked with social welfare and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.child_protection_service like '%linked_with_social_welfare_office%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-social-welfare-F-<1"
    grouping: "MVC"
    description: "Number of Female Children linked with social welfare and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.child_protection_service like '%linked_with_social_welfare_office%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-linked-mvcc-M-<1"
    grouping: "MVC"
    description: "Number of Male Children linked with MVCC/Child protection team and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.child_protection_service like '%linked_with_mvcc_child_protection_team%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-mvcc-F-<1"
    grouping: "MVC"
    description: "Number of Female Children linked with MVCC/Child protection team and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.child_protection_service like '%linked_with_mvcc_child_protection_team%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-protection-awareness-M-<1"
    grouping: "MVC"
    description: "Number of Male Children provided with general child protection awareness creation and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.child_protection_service like '%provided_with_general_child_protection_awareness_creation%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-child-protection-awareness-F-<1"
    grouping: "MVC"
    description: "Number of Female Children provided with general child protection awareness creation and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.child_protection_service like '%provided_with_general_child_protection_awareness_creation%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-health-services-M-<1"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on Health Related Services and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_provided like '%health_related_services_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-health-services-F-<1"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on Health Related Services and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_provided like '%health_related_services_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-health-services-M-<1"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on nutrition Services and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-health-services-F-<1"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on nutrition services and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"



  #Male
  - key: "mvc-referrals-nutrition-M-<1"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on nutrition Services and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-nutrition-F-<1"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on nutrition services and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-education-M-<1"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on nutrition Services and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_provided like '%education_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-education-F-<1"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on nutrition services and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_provided like '%education_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-child-protection-M-<1"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on child protection Services and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_provided like '%child_protection_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-child-protection-F-<1"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on child protection services and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_provided like '%child_protection_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-psychosocial-M-<1"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on psychosocial support Services and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_provided like '%psychosocial_support_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-psychosocial-F-<1"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on psychosocial support services and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_provided like '%psychosocial_support_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-economic-strengthening-M-<1"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on economic strengthening and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_provided like '%economic_strengthening_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-economic-strengthening-F-<1"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on economic strengthening and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_provided like '%economic_strengthening_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-others-M-<1"
    grouping: "MVC"
    description: "Number of Male Children provided other referrals are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_provided like '%other%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-others-F-<1"
    grouping: "MVC"
    description: "Number of Female Children provided other referrals and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_provided like '%other%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-completed-health-services-M-<1"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on health services are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_completed_by_client like '%health_related_services_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-health-services-F-<1"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on health services are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_completed_by_client like '%health_related_services_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-completed-nutrition-M-<1"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on health services are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_completed_by_client like '%nutrition_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-nutrition-F-<1"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on health services are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_completed_by_client like '%nutrition_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-completed-education-M-<1"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on education services are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_completed_by_client like '%education_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-education-F-<1"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on education services are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_completed_by_client like '%education_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-completed-child-protection-M-<1"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on child protection services are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_completed_by_client like '%child_protection_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-child-protection-F-<1"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on child protection services are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_completed_by_client like '%child_protection_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-completed-psychosocial-M-<1"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on psychosocial support services are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_completed_by_client like '%psychosocial_support_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-psychosocial-F-<1"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on  psychosocial support services are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_completed_by_client like '%psychosocial_support_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-referrals-completed-economic-strengthening-M-<1"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on economic strengthening services are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_completed_by_client like '%economic_strengthening_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-economic-strengthening-F-<1"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on economic strengthening are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_completed_by_client like '%economic_strengthening_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-completed-others-M-<1"
    grouping: "MVC"
    description: "Number of Male Children who completed other referrals and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_completed_by_client like '%other%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-others-F-<1"
    grouping: "MVC"
    description: "Number of Female Children who completed other referrals and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.referrals_completed_by_client like '%other%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referral-accompanied-to-services-M-<1"
    grouping: "MVC"
    description: "Number of Male Children who completed other referrals and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.support_provided_on_referrals like '%accompanied_to_service%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referral-accompanied-to-services-F-<1"
    grouping: "MVC"
    description: "Number of Female Children who completed other referrals and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.support_provided_on_referrals like '%accompanied_to_service%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referral-assisted-with-transport-M-<1"
    grouping: "MVC"
    description: "Number of Male Children Referrals assisted with transport and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.support_provided_on_referrals like '%assisted_with_transport%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referral-assisted-with-transport-F-<1"
    grouping: "MVC"
    description: "Number of Female Children Referrals assisted with transport and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.support_provided_on_referrals like '%assisted_with_transport%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-active-MVC-M-<1"
    grouping: "MVC"
    description: "Number of Male Active MVC who are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.visit_type = '%active_continuing_with_services%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-active-MVC-F-<1"
    grouping: "MVC"
    description: "Number of Female Active MVC who are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.visit_type = '%active_continuing_with_services%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-graduated-MVC-M-<1"
    grouping: "MVC"
    description: "Number of Male graduated MVC who are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.visit_type = '%graduated%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-graduated-MVC-F-<1"
    grouping: "MVC"
    description: "Number of Female graduated MVC who are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.visit_type = '%graduated%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-transferred-MVC-M-<1"
    grouping: "MVC"
    description: "Number of Male graduated MVC who are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.visit_type = '%transferred%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-transferred-MVC-F-<1"
    grouping: "MVC"
    description: "Number of Female graduated MVC who are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.visit_type = '%transferred%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-exited-without-graduation-M-<1"
    grouping: "MVC"
    description: "Number of Male who exited MVC without graduation and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.visit_type = '%exited_without_graduation%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-exited-without-graduation-F-<1"
    grouping: "MVC"
    description: "Number of Female who exited MVC without graduation and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.visit_type = '%exited_without_graduation%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-died-MVC-M-<1"
    grouping: "MVC"
    description: "Number of Male MVC who died and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.visit_type = '%died%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-died-MVC-F-<1"
    grouping: "MVC"
    description: "Number of Female MVC who died and are aged under 1"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND ecs.visit_type = '%died%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


######################################################Indicators for aged 1 to 4 #####################################################################################
  #Male
  - key: "mvc-number-of-children-enrolled-in-school-M-1-4"
    grouping: "MVC"
    description: "Number male children registered in school aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.enrollment_date / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-number-of-children-enrolled-in-school-F-1-4"
    grouping: "MVC"
    description: "Number Female children registered in school aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.enrollment_date / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-missed-school-M-1-4"
    grouping: "MVC"
    description: "Number of Male children who missed school for three or more days consecutively in a month aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.education_and_vocational_training = 'missed_school_three_days'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-missed-school-F-1-4"
    grouping: "MVC"
    description: "Number of Female children who missed school for three or more days consecutively in a month aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.education_and_vocational_training = 'missed_school_three_days'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-linked-to-education-support-M-1-4"
    grouping: "MVC"
    description: "Number of Male children who are linked to education and support and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.education_vocational_training like '%linked_to_education_support%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-linked-to-education-support-F-1-4"
    grouping: "MVC"
    description: "Number of Female children who are linked to education and support and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.education_vocational_training like '%linked_to_education_support%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-supported-vocational-training-M-1-4"
    grouping: "MVC"
    description: "Number of Male children who are linked to vocational training support and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.ecd_psychosocial_support like '%supported_attend_vocational_training%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-supported-vocational-training-F-1-4"
    grouping: "MVC"
    description: "Number of Female children who are linked to vocational trining support and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.ecd_psychosocial_support like '%supported_attend_vocational_training%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"



  #Male
  - key: "mvc-received-business-kit-M-1-4"
    grouping: "MVC"
    description: "Number of Male children who received business kit and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.ecd_psychosocial_support like '%received_business_start_up_kit%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-received-business-kit-F-1-4"
    grouping: "MVC"
    description: "Number of Female children who received business kit and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.ecd_psychosocial_support like '%received_business_start_up_kit%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-linked-to-memkwa-M-1-4"
    grouping: "MVC"
    description: "Number of Male children who received business kit and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.ecd_psychosocial_support like '%linked_mainstreamed_education_program%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-to-memkwa-F-1-4"
    grouping: "MVC"
    description: "Number of Female children who received business kit and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.ecd_psychosocial_support like '%linked_mainstreamed_education_program%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  ###############################################Education and vocational training ends here ###########################################################################

  ###############################################ECD & Psychosocial Support starts here ###########################################################################

  #Male
  - key: "mvc-attended-ecd-M-1-4"
    grouping: "MVC"
    description: "Number of Male children who attended ECD and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.ecd_psychosocial_support like '%attended_ecd_centre%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-attended-ecd-F-1-4"
    grouping: "MVC"
    description: "Number of Female children who received business kit and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.ecd_psychosocial_support like '%attended_ecd_centre%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-attended-childrens-clubs-M-1-4"
    grouping: "MVC"
    description: "Number of Male children attended children's clubs and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.ecd_psychosocial_support like '%children_attended_childrens_clubs%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-attended-childrens-clubs-F-1-4"
    grouping: "MVC"
    description: "Number of Female children attended children's clubs and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.ecd_psychosocial_support like '%children_attended_childrens_clubs%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-attended-teen-clubs-M-1-4"
    grouping: "MVC"
    description: "Number of Male children attended teen's clubs and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.ecd_psychosocial_support like '%children_attended_teen_clubs%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-attended-teen-clubs-F-1-4"
    grouping: "MVC"
    description: "Number of Female children attended teen's clubs and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.ecd_psychosocial_support like '%children_attended_teen_clubs%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  ############################################### MVC<5 years Vaccination  ###########################################################################

  #Male
  - key: "mvc-vaccinated-M-1-4"
    grouping: "MVC"
    description: "Number of Male MVC < 5 years received required Vaccination and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.child_received_vaccinations = 'yes'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-vaccinated-F-1-4"
    grouping: "MVC"
    description: "Number of Female MVC < 5 years received required Vaccination and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.child_received_vaccinations = 'yes'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-not-vaccinated-M-1-4"
    grouping: "MVC"
    description: "Number of Male MVC < 5 years who did not receive required Vaccination and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.child_received_vaccinations = 'no'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-not-vaccinated-F-1-4"
    grouping: "MVC"
    description: "Number of Female MVC < 5 years who did not receive required Vaccination and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.child_received_vaccinations = 'no'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  ############################################### Nutritional Status  ###########################################################################

  #Male
  - key: "mvc-severe-malnutrition-M-1-4"
    grouping: "MVC"
    description: "Number of Male MVC with Severe Malnutrition and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.child_nutritional_status = 'severe_malnutrition'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-severe-malnutrition-F-1-4"
    grouping: "MVC"
    description: "Number of Female MVC with Severe Malnutrition and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.child_nutritional_status = 'severe_malnutrition'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-moderate-malnutrition-M-1-4"
    grouping: "MVC"
    description: "Number of Male MVC with Severe Malnutrition and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.child_nutritional_status = 'moderate_malnutrition'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-moderate-malnutrition-M-1-4"
    grouping: "MVC"
    description: "Number of Female MVC with Severe Malnutrition and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.child_nutritional_status = 'moderate_malnutrition'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  ############################################### Health Care  ###########################################################################

  #Male
  - key: "mvc-joined-chf-tika-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children supported to join CHF/TIKA and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.health_care_service_provided like '%supported_to_join_chf_tika%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-joined-chf-tika-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children supported to join CHF/TIKA and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.health_care_service_provided like '%supported_to_join_chf_tika%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-hiv-prevention-counseling-M-1-4"
    grouping: "MVC"
    description: "Number of Male children received HIV prevention counseling services and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.health_care_service_provided like '%hiv_prevention_counseling_service%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-hiv-prevention-counseling-F-1-4"
    grouping: "MVC"
    description: "Number of Female children received HIV prevention counseling services and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.health_care_service_provided like '%hiv_prevention_counseling_service%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-disclosure-support-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children received disclosure support and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.health_care_service_provided like '%disclosure_support%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-disclosure-support-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children received disclosure support and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.health_care_service_provided like '%disclosure_support%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"



  #Male
  - key: "mvc-linked-hiv-support-group-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children linked to HIV support group and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.health_care_service_provided like '%hiv_support_group%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-hiv-support-group-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children linked to HIV support group and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.health_care_service_provided like '%hiv_support_group%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"





  #Male
  - key: "mvc-art-adherence-counseling-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children received ART adherence counseling and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.health_care_service_provided like '%art_adherence_counseling%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-art-adherence-counseling-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children received ART adherence counseling and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.health_care_service_provided like '%art_adherence_counseling%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-hiv-risk-assessment-M-1-4"
    grouping: "MVC"
    description: "Number of Male children received HIV risk assessment and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.health_care_service_provided like '%hiv_risk_assessment%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-hiv-risk-assessment-F-1-4"
    grouping: "MVC"
    description: "Number of Female children received HIV risk assessment and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.health_care_service_provided like '%hiv_risk_assessment%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-hiv-prevention-education-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children provided with HIV prevention education and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.health_care_service_provided like '%hiv_prevention_education%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-hiv-prevention-education-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children provided with HIV prevention education and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.health_care_service_provided like '%hiv_prevention_education%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-supported-birth-certificates-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children supported to get birth certificates and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.child_protection_service like '%supported_to_get_birth_certificates%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-supported-birth-certificates-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children supported to get birth certificates and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.child_protection_service like '%supported_to_get_birth_certificates%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-emergency-care-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children supported to get emergency care and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.child_protection_service like '%provided_with_emergency_care_and_support_services%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-emergency-care-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children supported to get emergency care and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.child_protection_service like '%provided_with_emergency_care_and_support_services%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-linked-social-welfare-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children linked with social welfare and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.child_protection_service like '%linked_with_social_welfare_office%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-social-welfare-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children linked with social welfare and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.child_protection_service like '%linked_with_social_welfare_office%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-linked-mvcc-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children linked with MVCC/Child protection team and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.child_protection_service like '%linked_with_mvcc_child_protection_team%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-mvcc-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children linked with MVCC/Child protection team and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.child_protection_service like '%linked_with_mvcc_child_protection_team%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-protection-awareness-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children provided with general child protection awareness creation and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.child_protection_service like '%provided_with_general_child_protection_awareness_creation%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-child-protection-awareness-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children provided with general child protection awareness creation and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.child_protection_service like '%provided_with_general_child_protection_awareness_creation%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-health-services-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on Health Related Services and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_provided like '%health_related_services_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-health-services-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on Health Related Services and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_provided like '%health_related_services_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-health-services-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on nutrition Services and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-health-services-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on nutrition services and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"



  #Male
  - key: "mvc-referrals-nutrition-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on nutrition Services and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-nutrition-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on nutrition services and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-education-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on nutrition Services and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_provided like '%education_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-education-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on nutrition services and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_provided like '%education_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-child-protection-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on child protection Services and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_provided like '%child_protection_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-child-protection-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on child protection services and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_provided like '%child_protection_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-psychosocial-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on psychosocial support Services and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_provided like '%psychosocial_support_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-psychosocial-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on psychosocial support services and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_provided like '%psychosocial_support_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-economic-strengthening-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on economic strengthening and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_provided like '%economic_strengthening_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-economic-strengthening-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on economic strengthening and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_provided like '%economic_strengthening_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-others-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children provided other referrals are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_provided like '%other%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-others-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children provided other referrals and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_provided like '%other%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-completed-health-services-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on health services are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_completed_by_client like '%health_related_services_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-health-services-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on health services are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_completed_by_client like '%health_related_services_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-completed-nutrition-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on health services are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_completed_by_client like '%nutrition_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-nutrition-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on health services are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_completed_by_client like '%nutrition_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-completed-education-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on education services are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_completed_by_client like '%education_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-education-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on education services are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_completed_by_client like '%education_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-completed-child-protection-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on child protection services are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_completed_by_client like '%child_protection_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-child-protection-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on child protection services are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_completed_by_client like '%child_protection_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-completed-psychosocial-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on psychosocial support services are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_completed_by_client like '%psychosocial_support_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-psychosocial-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on  psychosocial support services are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_completed_by_client like '%psychosocial_support_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-referrals-completed-economic-strengthening-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on economic strengthening services are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_completed_by_client like '%economic_strengthening_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-economic-strengthening-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on economic strengthening are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_completed_by_client like '%economic_strengthening_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-completed-others-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children who completed other referrals and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_completed_by_client like '%other%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-others-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children who completed other referrals and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.referrals_completed_by_client like '%other%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referral-accompanied-to-services-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children who completed other referrals and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.support_provided_on_referrals like '%accompanied_to_service%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referral-accompanied-to-services-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children who completed other referrals and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.support_provided_on_referrals like '%accompanied_to_service%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referral-assisted-with-transport-M-1-4"
    grouping: "MVC"
    description: "Number of Male Children Referrals assisted with transport and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.support_provided_on_referrals like '%assisted_with_transport%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referral-assisted-with-transport-F-1-4"
    grouping: "MVC"
    description: "Number of Female Children Referrals assisted with transport and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.support_provided_on_referrals like '%assisted_with_transport%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-active-MVC-M-1-4"
    grouping: "MVC"
    description: "Number of Male Active MVC who are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.visit_type = '%active_continuing_with_services%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-active-MVC-F-1-4"
    grouping: "MVC"
    description: "Number of Female Active MVC who are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.visit_type = '%active_continuing_with_services%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-graduated-MVC-M-1-4"
    grouping: "MVC"
    description: "Number of Male graduated MVC who are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.visit_type = '%graduated%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-graduated-MVC-F-1-4"
    grouping: "MVC"
    description: "Number of Female graduated MVC who are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.visit_type = '%graduated%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-transferred-MVC-M-1-4"
    grouping: "MVC"
    description: "Number of Male graduated MVC who are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.visit_type = '%transferred%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-transferred-MVC-F-1-4"
    grouping: "MVC"
    description: "Number of Female graduated MVC who are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.visit_type = '%transferred%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-exited-without-graduation-M-1-4"
    grouping: "MVC"
    description: "Number of Male who exited MVC without graduation and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.visit_type = '%exited_without_graduation%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-exited-without-graduation-F-<1"
    grouping: "MVC"
    description: "Number of Female who exited MVC without graduation and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.visit_type = '%exited_without_graduation%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-died-MVC-M-1-4"
    grouping: "MVC"
    description: "Number of Male MVC who died and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.visit_type = '%died%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-died-MVC-F-1-4"
    grouping: "MVC"
    description: "Number of Female MVC who died and are aged from 1 to 4"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND ecs.visit_type = '%died%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


######################################################Indicators for aged 5 to 9 #####################################################################################
  #Male
  - key: "mvc-number-of-children-enrolled-in-school-M-5-9"
    grouping: "MVC"
    description: "Number male children registered in school aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.enrollment_date / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-number-of-children-enrolled-in-school-F-5-9"
    grouping: "MVC"
    description: "Number Female children registered in school aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.enrollment_date / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-missed-school-M-5-9"
    grouping: "MVC"
    description: "Number of Male children who missed school for three or more days consecutively in a month aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.education_and_vocational_training = 'missed_school_three_days'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-missed-school-F-5-9"
    grouping: "MVC"
    description: "Number of Female children who missed school for three or more days consecutively in a month aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.education_and_vocational_training = 'missed_school_three_days'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-linked-to-education-support-M-5-9"
    grouping: "MVC"
    description: "Number of Male children who are linked to education and support and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.education_vocational_training like '%linked_to_education_support%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-linked-to-education-support-F-5-9"
    grouping: "MVC"
    description: "Number of Female children who are linked to education and support and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.education_vocational_training like '%linked_to_education_support%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-supported-vocational-training-M-5-9"
    grouping: "MVC"
    description: "Number of Male children who are linked to vocational training support and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%supported_attend_vocational_training%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-supported-vocational-training-F-5-9"
    grouping: "MVC"
    description: "Number of Female children who are linked to vocational trining support and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%supported_attend_vocational_training%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"



  #Male
  - key: "mvc-received-business-kit-M-5-9"
    grouping: "MVC"
    description: "Number of Male children who received business kit and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%received_business_start_up_kit%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-received-business-kit-F-5-9"
    grouping: "MVC"
    description: "Number of Female children who received business kit and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%received_business_start_up_kit%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-linked-to-memkwa-M-5-9"
    grouping: "MVC"
    description: "Number of Male children who received business kit and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%linked_mainstreamed_education_program%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-to-memkwa-F-5-9"
    grouping: "MVC"
    description: "Number of Female children who received business kit and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%linked_mainstreamed_education_program%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  ###############################################Education and vocational training ends here ###########################################################################

  ###############################################ECD & Psychosocial Support starts here ###########################################################################

  #Male
  - key: "mvc-attended-ecd-M-5-9"
    grouping: "MVC"
    description: "Number of Male children who attended ECD and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%attended_ecd_centre%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-attended-ecd-F-5-9"
    grouping: "MVC"
    description: "Number of Female children who received business kit and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%attended_ecd_centre%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-attended-childrens-clubs-M-5-9"
    grouping: "MVC"
    description: "Number of Male children attended children's clubs and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%children_attended_childrens_clubs%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-attended-childrens-clubs-F-5-9"
    grouping: "MVC"
    description: "Number of Female children attended children's clubs and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%children_attended_childrens_clubs%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-attended-teen-clubs-M-5-9"
    grouping: "MVC"
    description: "Number of Male children attended teen's clubs and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%children_attended_teen_clubs%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-attended-teen-clubs-F-5-9"
    grouping: "MVC"
    description: "Number of Female children attended teen's clubs and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%children_attended_teen_clubs%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  ############################################### MVC<5 years Vaccination  ###########################################################################

  #Male
  - key: "mvc-vaccinated-M-5-9"
    grouping: "MVC"
    description: "Number of Male MVC < 5 years received required Vaccination and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_received_vaccinations = 'yes'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-vaccinated-F-5-9"
    grouping: "MVC"
    description: "Number of Female MVC < 5 years received required Vaccination and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_received_vaccinations = 'yes'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-not-vaccinated-M-5-9"
    grouping: "MVC"
    description: "Number of Male MVC < 5 years who did not receive required Vaccination and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_received_vaccinations = 'no'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-not-vaccinated-F-5-9"
    grouping: "MVC"
    description: "Number of Female MVC < 5 years who did not receive required Vaccination and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_received_vaccinations = 'no'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  ############################################### Nutritional Status  ###########################################################################

  #Male
  - key: "mvc-severe-malnutrition-M-5-9"
    grouping: "MVC"
    description: "Number of Male MVC with Severe Malnutrition and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_nutritional_status = 'severe_malnutrition'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-severe-malnutrition-F-5-9"
    grouping: "MVC"
    description: "Number of Female MVC with Severe Malnutrition and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_nutritional_status = 'severe_malnutrition'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-moderate-malnutrition-M-5-9"
    grouping: "MVC"
    description: "Number of Male MVC with Severe Malnutrition and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_nutritional_status = 'moderate_malnutrition'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-moderate-malnutrition-M-5-9"
    grouping: "MVC"
    description: "Number of Female MVC with Severe Malnutrition and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_nutritional_status = 'moderate_malnutrition'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  ############################################### Health Care  ###########################################################################

  #Male
  - key: "mvc-joined-chf-tika-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children supported to join CHF/TIKA and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%supported_to_join_chf_tika%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-joined-chf-tika-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children supported to join CHF/TIKA and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%supported_to_join_chf_tika%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-hiv-prevention-counseling-M-5-9"
    grouping: "MVC"
    description: "Number of Male children received HIV prevention counseling services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%hiv_prevention_counseling_service%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-hiv-prevention-counseling-F-5-9"
    grouping: "MVC"
    description: "Number of Female children received HIV prevention counseling services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%hiv_prevention_counseling_service%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-disclosure-support-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children received disclosure support and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%disclosure_support%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-disclosure-support-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children received disclosure support and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%disclosure_support%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"



  #Male
  - key: "mvc-linked-hiv-support-group-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children linked to HIV support group and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%hiv_support_group%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-hiv-support-group-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children linked to HIV support group and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%hiv_support_group%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"





  #Male
  - key: "mvc-art-adherence-counseling-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children received ART adherence counseling and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%art_adherence_counseling%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-art-adherence-counseling-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children received ART adherence counseling and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%art_adherence_counseling%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-hiv-risk-assessment-M-5-9"
    grouping: "MVC"
    description: "Number of Male children received HIV risk assessment and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%hiv_risk_assessment%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-hiv-risk-assessment-F-5-9"
    grouping: "MVC"
    description: "Number of Female children received HIV risk assessment and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%hiv_risk_assessment%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-hiv-prevention-education-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children provided with HIV prevention education and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%hiv_prevention_education%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-hiv-prevention-education-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children provided with HIV prevention education and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%hiv_prevention_education%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-supported-birth-certificates-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children supported to get birth certificates and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_protection_service like '%supported_to_get_birth_certificates%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-supported-birth-certificates-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children supported to get birth certificates and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_protection_service like '%supported_to_get_birth_certificates%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-emergency-care-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children supported to get emergency care and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_protection_service like '%provided_with_emergency_care_and_support_services%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-emergency-care-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children supported to get emergency care and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_protection_service like '%provided_with_emergency_care_and_support_services%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-linked-social-welfare-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children linked with social welfare and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_protection_service like '%linked_with_social_welfare_office%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-social-welfare-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children linked with social welfare and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_protection_service like '%linked_with_social_welfare_office%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-linked-mvcc-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children linked with MVCC/Child protection team and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_protection_service like '%linked_with_mvcc_child_protection_team%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-mvcc-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children linked with MVCC/Child protection team and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_protection_service like '%linked_with_mvcc_child_protection_team%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-protection-awareness-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children provided with general child protection awareness creation and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_protection_service like '%provided_with_general_child_protection_awareness_creation%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-child-protection-awareness-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children provided with general child protection awareness creation and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_protection_service like '%provided_with_general_child_protection_awareness_creation%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-health-services-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on Health Related Services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%health_related_services_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-health-services-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on Health Related Services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%health_related_services_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-health-services-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on nutrition Services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-health-services-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on nutrition services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"



  #Male
  - key: "mvc-referrals-nutrition-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on nutrition Services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-nutrition-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on nutrition services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-education-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on nutrition Services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%education_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-education-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on nutrition services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%education_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-child-protection-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on child protection Services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%child_protection_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-child-protection-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on child protection services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%child_protection_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-psychosocial-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on psychosocial support Services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%psychosocial_support_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-psychosocial-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on psychosocial support services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%psychosocial_support_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-economic-strengthening-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on economic strengthening and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%economic_strengthening_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-economic-strengthening-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on economic strengthening and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%economic_strengthening_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-others-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children provided other referrals are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%other%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-others-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children provided other referrals and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%other%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-completed-health-services-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on health services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%health_related_services_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-health-services-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on health services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%health_related_services_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-completed-nutrition-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on health services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%nutrition_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-nutrition-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on health services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%nutrition_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-completed-education-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on education services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%education_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-education-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on education services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%education_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-completed-child-protection-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on child protection services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%child_protection_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-child-protection-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on child protection services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%child_protection_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-completed-psychosocial-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on psychosocial support services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%psychosocial_support_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-psychosocial-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on  psychosocial support services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%psychosocial_support_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-referrals-completed-economic-strengthening-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on economic strengthening services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%economic_strengthening_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-economic-strengthening-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on economic strengthening are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%economic_strengthening_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-completed-others-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children who completed other referrals and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%other%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-others-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children who completed other referrals and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%other%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referral-accompanied-to-services-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children who completed other referrals and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.support_provided_on_referrals like '%accompanied_to_service%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referral-accompanied-to-services-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children who completed other referrals and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.support_provided_on_referrals like '%accompanied_to_service%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referral-assisted-with-transport-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children Referrals assisted with transport and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.support_provided_on_referrals like '%assisted_with_transport%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referral-assisted-with-transport-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children Referrals assisted with transport and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.support_provided_on_referrals like '%assisted_with_transport%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-active-MVC-M-5-9"
    grouping: "MVC"
    description: "Number of Male Active MVC who are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.visit_type = '%active_continuing_with_services%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-active-MVC-F-5-9"
    grouping: "MVC"
    description: "Number of Female Active MVC who are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.visit_type = '%active_continuing_with_services%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-graduated-MVC-M-5-9"
    grouping: "MVC"
    description: "Number of Male graduated MVC who are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.visit_type = '%graduated%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-graduated-MVC-F-5-9"
    grouping: "MVC"
    description: "Number of Female graduated MVC who are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.visit_type = '%graduated%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-transferred-MVC-M-5-9"
    grouping: "MVC"
    description: "Number of Male graduated MVC who are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.visit_type = '%transferred%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-transferred-MVC-F-5-9"
    grouping: "MVC"
    description: "Number of Female graduated MVC who are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.visit_type = '%transferred%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-exited-without-graduation-M-5-9"
    grouping: "MVC"
    description: "Number of Male who exited MVC without graduation and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.visit_type = '%exited_without_graduation%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-exited-without-graduation-F-<1"
    grouping: "MVC"
    description: "Number of Female who exited MVC without graduation and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.visit_type = '%exited_without_graduation%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-died-MVC-M-5-9"
    grouping: "MVC"
    description: "Number of Male MVC who died and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.visit_type = '%died%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-died-MVC-F-5-9"
    grouping: "MVC"
    description: "Number of Female MVC who died and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.visit_type = '%died%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


######################################################Indicators for aged 5 to 9 #####################################################################################
  #Male
  - key: "mvc-number-of-children-enrolled-in-school-M-5-9"
    grouping: "MVC"
    description: "Number male children registered in school aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.enrollment_date / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-number-of-children-enrolled-in-school-F-5-9"
    grouping: "MVC"
    description: "Number Female children registered in school aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.enrollment_date / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-missed-school-M-5-9"
    grouping: "MVC"
    description: "Number of Male children who missed school for three or more days consecutively in a month aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.education_and_vocational_training = 'missed_school_three_days'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-missed-school-F-5-9"
    grouping: "MVC"
    description: "Number of Female children who missed school for three or more days consecutively in a month aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.education_and_vocational_training = 'missed_school_three_days'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-linked-to-education-support-M-5-9"
    grouping: "MVC"
    description: "Number of Male children who are linked to education and support and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.education_vocational_training like '%linked_to_education_support%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-linked-to-education-support-F-5-9"
    grouping: "MVC"
    description: "Number of Female children who are linked to education and support and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.education_vocational_training like '%linked_to_education_support%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-supported-vocational-training-M-5-9"
    grouping: "MVC"
    description: "Number of Male children who are linked to vocational training support and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%supported_attend_vocational_training%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-supported-vocational-training-F-5-9"
    grouping: "MVC"
    description: "Number of Female children who are linked to vocational trining support and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%supported_attend_vocational_training%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"



  #Male
  - key: "mvc-received-business-kit-M-5-9"
    grouping: "MVC"
    description: "Number of Male children who received business kit and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%received_business_start_up_kit%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-received-business-kit-F-5-9"
    grouping: "MVC"
    description: "Number of Female children who received business kit and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%received_business_start_up_kit%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-linked-to-memkwa-M-5-9"
    grouping: "MVC"
    description: "Number of Male children who received business kit and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%linked_mainstreamed_education_program%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-to-memkwa-F-5-9"
    grouping: "MVC"
    description: "Number of Female children who received business kit and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%linked_mainstreamed_education_program%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  ###############################################Education and vocational training ends here ###########################################################################

  ###############################################ECD & Psychosocial Support starts here ###########################################################################

  #Male
  - key: "mvc-attended-ecd-M-5-9"
    grouping: "MVC"
    description: "Number of Male children who attended ECD and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%attended_ecd_centre%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-attended-ecd-F-5-9"
    grouping: "MVC"
    description: "Number of Female children who received business kit and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%attended_ecd_centre%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-attended-childrens-clubs-M-5-9"
    grouping: "MVC"
    description: "Number of Male children attended children's clubs and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%children_attended_childrens_clubs%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-attended-childrens-clubs-F-5-9"
    grouping: "MVC"
    description: "Number of Female children attended children's clubs and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%children_attended_childrens_clubs%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-attended-teen-clubs-M-5-9"
    grouping: "MVC"
    description: "Number of Male children attended teen's clubs and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%children_attended_teen_clubs%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-attended-teen-clubs-F-5-9"
    grouping: "MVC"
    description: "Number of Female children attended teen's clubs and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.ecd_psychosocial_support like '%children_attended_teen_clubs%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  ############################################### MVC<5 years Vaccination  ###########################################################################

  #Male
  - key: "mvc-vaccinated-M-5-9"
    grouping: "MVC"
    description: "Number of Male MVC < 5 years received required Vaccination and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_received_vaccinations = 'yes'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-vaccinated-F-5-9"
    grouping: "MVC"
    description: "Number of Female MVC < 5 years received required Vaccination and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_received_vaccinations = 'yes'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-not-vaccinated-M-5-9"
    grouping: "MVC"
    description: "Number of Male MVC < 5 years who did not receive required Vaccination and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_received_vaccinations = 'no'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-not-vaccinated-F-5-9"
    grouping: "MVC"
    description: "Number of Female MVC < 5 years who did not receive required Vaccination and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_received_vaccinations = 'no'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  ############################################### Nutritional Status  ###########################################################################

  #Male
  - key: "mvc-severe-malnutrition-M-5-9"
    grouping: "MVC"
    description: "Number of Male MVC with Severe Malnutrition and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_nutritional_status = 'severe_malnutrition'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-severe-malnutrition-F-5-9"
    grouping: "MVC"
    description: "Number of Female MVC with Severe Malnutrition and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_nutritional_status = 'severe_malnutrition'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-moderate-malnutrition-M-5-9"
    grouping: "MVC"
    description: "Number of Male MVC with Severe Malnutrition and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_nutritional_status = 'moderate_malnutrition'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-moderate-malnutrition-M-5-9"
    grouping: "MVC"
    description: "Number of Female MVC with Severe Malnutrition and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_nutritional_status = 'moderate_malnutrition'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  ############################################### Health Care  ###########################################################################

  #Male
  - key: "mvc-joined-chf-tika-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children supported to join CHF/TIKA and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%supported_to_join_chf_tika%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-joined-chf-tika-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children supported to join CHF/TIKA and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%supported_to_join_chf_tika%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-hiv-prevention-counseling-M-5-9"
    grouping: "MVC"
    description: "Number of Male children received HIV prevention counseling services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%hiv_prevention_counseling_service%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-hiv-prevention-counseling-F-5-9"
    grouping: "MVC"
    description: "Number of Female children received HIV prevention counseling services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%hiv_prevention_counseling_service%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-disclosure-support-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children received disclosure support and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%disclosure_support%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-disclosure-support-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children received disclosure support and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%disclosure_support%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-linked-hiv-support-group-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children linked to HIV support group and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%hiv_support_group%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-hiv-support-group-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children linked to HIV support group and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%hiv_support_group%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-art-adherence-counseling-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children received ART adherence counseling and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%art_adherence_counseling%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-art-adherence-counseling-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children received ART adherence counseling and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%art_adherence_counseling%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-hiv-risk-assessment-M-5-9"
    grouping: "MVC"
    description: "Number of Male children received HIV risk assessment and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%hiv_risk_assessment%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-hiv-risk-assessment-F-5-9"
    grouping: "MVC"
    description: "Number of Female children received HIV risk assessment and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%hiv_risk_assessment%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-hiv-prevention-education-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children provided with HIV prevention education and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%hiv_prevention_education%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-hiv-prevention-education-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children provided with HIV prevention education and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.health_care_service_provided like '%hiv_prevention_education%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-supported-birth-certificates-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children supported to get birth certificates and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_protection_service like '%supported_to_get_birth_certificates%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-supported-birth-certificates-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children supported to get birth certificates and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_protection_service like '%supported_to_get_birth_certificates%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-emergency-care-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children supported to get emergency care and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_protection_service like '%provided_with_emergency_care_and_support_services%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-emergency-care-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children supported to get emergency care and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_protection_service like '%provided_with_emergency_care_and_support_services%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-linked-social-welfare-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children linked with social welfare and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_protection_service like '%linked_with_social_welfare_office%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-social-welfare-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children linked with social welfare and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_protection_service like '%linked_with_social_welfare_office%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-linked-mvcc-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children linked with MVCC/Child protection team and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_protection_service like '%linked_with_mvcc_child_protection_team%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-mvcc-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children linked with MVCC/Child protection team and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_protection_service like '%linked_with_mvcc_child_protection_team%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-protection-awareness-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children provided with general child protection awareness creation and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_protection_service like '%provided_with_general_child_protection_awareness_creation%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-child-protection-awareness-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children provided with general child protection awareness creation and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.child_protection_service like '%provided_with_general_child_protection_awareness_creation%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-health-services-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on Health Related Services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%health_related_services_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-health-services-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on Health Related Services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%health_related_services_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-health-services-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on nutrition Services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-health-services-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on nutrition services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"



  #Male
  - key: "mvc-referrals-nutrition-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on nutrition Services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-nutrition-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on nutrition services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-education-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on nutrition Services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%education_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-education-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on nutrition services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%education_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-child-protection-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on child protection Services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%child_protection_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-child-protection-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on child protection services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%child_protection_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-psychosocial-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on psychosocial support Services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%psychosocial_support_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-psychosocial-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on psychosocial support services and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%psychosocial_support_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-economic-strengthening-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on economic strengthening and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%economic_strengthening_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-economic-strengthening-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on economic strengthening and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%economic_strengthening_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-others-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children provided other referrals are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%other%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-others-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children provided other referrals and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_provided like '%other%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-completed-health-services-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on health services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%health_related_services_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-health-services-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on health services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%health_related_services_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-completed-nutrition-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on health services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%nutrition_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-nutrition-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on health services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%nutrition_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-completed-education-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on education services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%education_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-education-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on education services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%education_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-completed-child-protection-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on child protection services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%child_protection_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-child-protection-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on child protection services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%child_protection_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-completed-psychosocial-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on psychosocial support services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%psychosocial_support_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-psychosocial-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on  psychosocial support services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%psychosocial_support_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-referrals-completed-economic-strengthening-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on economic strengthening services are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%economic_strengthening_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-economic-strengthening-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on economic strengthening are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%economic_strengthening_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-completed-others-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children who completed other referrals and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%other%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-others-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children who completed other referrals and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.referrals_completed_by_client like '%other%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referral-accompanied-to-services-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children who completed other referrals and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.support_provided_on_referrals like '%accompanied_to_service%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referral-accompanied-to-services-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children who completed other referrals and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.support_provided_on_referrals like '%accompanied_to_service%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referral-assisted-with-transport-M-5-9"
    grouping: "MVC"
    description: "Number of Male Children Referrals assisted with transport and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.support_provided_on_referrals like '%assisted_with_transport%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referral-assisted-with-transport-F-5-9"
    grouping: "MVC"
    description: "Number of Female Children Referrals assisted with transport and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.support_provided_on_referrals like '%assisted_with_transport%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-active-MVC-M-5-9"
    grouping: "MVC"
    description: "Number of Male Active MVC who are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.visit_type = '%active_continuing_with_services%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-active-MVC-F-5-9"
    grouping: "MVC"
    description: "Number of Female Active MVC who are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.visit_type = '%active_continuing_with_services%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-graduated-MVC-M-5-9"
    grouping: "MVC"
    description: "Number of Male graduated MVC who are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.visit_type = '%graduated%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-graduated-MVC-F-5-9"
    grouping: "MVC"
    description: "Number of Female graduated MVC who are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.visit_type = '%graduated%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-transferred-MVC-M-5-9"
    grouping: "MVC"
    description: "Number of Male graduated MVC who are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.visit_type = '%transferred%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-transferred-MVC-F-5-9"
    grouping: "MVC"
    description: "Number of Female graduated MVC who are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.visit_type = '%transferred%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-exited-without-graduation-M-5-9"
    grouping: "MVC"
    description: "Number of Male who exited MVC without graduation and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.visit_type = '%exited_without_graduation%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-exited-without-graduation-F-<1"
    grouping: "MVC"
    description: "Number of Female who exited MVC without graduation and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.visit_type = '%exited_without_graduation%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-died-MVC-M-5-9"
    grouping: "MVC"
    description: "Number of Male MVC who died and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.visit_type = '%died%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-died-MVC-F-5-9"
    grouping: "MVC"
    description: "Number of Female MVC who died and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 5 AND (date('Now') - date(efm.dob)) <= 9
                      AND ecs.visit_type = '%died%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


######################################################Indicators for aged 10 to 14 #####################################################################################
  #Male
  - key: "mvc-number-of-children-enrolled-in-school-M-10-14"
    grouping: "MVC"
    description: "Number male children registered in school aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.enrollment_date / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-number-of-children-enrolled-in-school-F-10-14"
    grouping: "MVC"
    description: "Number Female children registered in school aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.enrollment_date / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-missed-school-M-10-14"
    grouping: "MVC"
    description: "Number of Male children who missed school for three or more days consecutively in a month aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.education_and_vocational_training = 'missed_school_three_days'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-missed-school-F-10-14"
    grouping: "MVC"
    description: "Number of Female children who missed school for three or more days consecutively in a month aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.education_and_vocational_training = 'missed_school_three_days'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-linked-to-education-support-M-10-14"
    grouping: "MVC"
    description: "Number of Male children who are linked to education and support and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.education_vocational_training like '%linked_to_education_support%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-linked-to-education-support-F-10-14"
    grouping: "MVC"
    description: "Number of Female children who are linked to education and support and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.education_vocational_training like '%linked_to_education_support%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-supported-vocational-training-M-10-14"
    grouping: "MVC"
    description: "Number of Male children who are linked to vocational training support and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.ecd_psychosocial_support like '%supported_attend_vocational_training%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-supported-vocational-training-F-10-14"
    grouping: "MVC"
    description: "Number of Female children who are linked to vocational trining support and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.ecd_psychosocial_support like '%supported_attend_vocational_training%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"



  #Male
  - key: "mvc-received-business-kit-M-10-14"
    grouping: "MVC"
    description: "Number of Male children who received business kit and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.ecd_psychosocial_support like '%received_business_start_up_kit%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-received-business-kit-F-10-14"
    grouping: "MVC"
    description: "Number of Female children who received business kit and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.ecd_psychosocial_support like '%received_business_start_up_kit%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-linked-to-memkwa-M-10-14"
    grouping: "MVC"
    description: "Number of Male children who received business kit and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.ecd_psychosocial_support like '%linked_mainstreamed_education_program%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-to-memkwa-F-10-14"
    grouping: "MVC"
    description: "Number of Female children who received business kit and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.ecd_psychosocial_support like '%linked_mainstreamed_education_program%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  ###############################################Education and vocational training ends here ###########################################################################

  ###############################################ECD & Psychosocial Support starts here ###########################################################################

  #Male
  - key: "mvc-attended-ecd-M-10-14"
    grouping: "MVC"
    description: "Number of Male children who attended ECD and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.ecd_psychosocial_support like '%attended_ecd_centre%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-attended-ecd-F-10-14"
    grouping: "MVC"
    description: "Number of Female children who received business kit and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.ecd_psychosocial_support like '%attended_ecd_centre%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-attended-childrens-clubs-M-10-14"
    grouping: "MVC"
    description: "Number of Male children attended children's clubs and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.ecd_psychosocial_support like '%children_attended_childrens_clubs%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-attended-childrens-clubs-F-10-14"
    grouping: "MVC"
    description: "Number of Female children attended children's clubs and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.ecd_psychosocial_support like '%children_attended_childrens_clubs%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-attended-teen-clubs-M-10-14"
    grouping: "MVC"
    description: "Number of Male children attended teen's clubs and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.ecd_psychosocial_support like '%children_attended_teen_clubs%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-attended-teen-clubs-F-10-14"
    grouping: "MVC"
    description: "Number of Female children attended teen's clubs and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.ecd_psychosocial_support like '%children_attended_teen_clubs%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  ############################################### MVC<5 years Vaccination  ###########################################################################

  #Male
  - key: "mvc-vaccinated-M-10-14"
    grouping: "MVC"
    description: "Number of Male MVC < 5 years received required Vaccination and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.child_received_vaccinations = 'yes'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-vaccinated-F-10-14"
    grouping: "MVC"
    description: "Number of Female MVC < 5 years received required Vaccination and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.child_received_vaccinations = 'yes'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-not-vaccinated-M-10-14"
    grouping: "MVC"
    description: "Number of Male MVC < 5 years who did not receive required Vaccination and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.child_received_vaccinations = 'no'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-not-vaccinated-F-10-14"
    grouping: "MVC"
    description: "Number of Female MVC < 5 years who did not receive required Vaccination and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.child_received_vaccinations = 'no'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  ############################################### Nutritional Status  ###########################################################################

  #Male
  - key: "mvc-severe-malnutrition-M-10-14"
    grouping: "MVC"
    description: "Number of Male MVC with Severe Malnutrition and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.child_nutritional_status = 'severe_malnutrition'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-severe-malnutrition-F-10-14"
    grouping: "MVC"
    description: "Number of Female MVC with Severe Malnutrition and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.child_nutritional_status = 'severe_malnutrition'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-moderate-malnutrition-M-10-14"
    grouping: "MVC"
    description: "Number of Male MVC with Severe Malnutrition and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.child_nutritional_status = 'moderate_malnutrition'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-moderate-malnutrition-M-10-14"
    grouping: "MVC"
    description: "Number of Female MVC with Severe Malnutrition and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.child_nutritional_status = 'moderate_malnutrition'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  ############################################### Health Care  ###########################################################################

  #Male
  - key: "mvc-joined-chf-tika-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children supported to join CHF/TIKA and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.health_care_service_provided like '%supported_to_join_chf_tika%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-joined-chf-tika-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children supported to join CHF/TIKA and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.health_care_service_provided like '%supported_to_join_chf_tika%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-hiv-prevention-counseling-M-10-14"
    grouping: "MVC"
    description: "Number of Male children received HIV prevention counseling services and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.health_care_service_provided like '%hiv_prevention_counseling_service%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-hiv-prevention-counseling-F-10-14"
    grouping: "MVC"
    description: "Number of Female children received HIV prevention counseling services and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.health_care_service_provided like '%hiv_prevention_counseling_service%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-disclosure-support-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children received disclosure support and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.health_care_service_provided like '%disclosure_support%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-disclosure-support-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children received disclosure support and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.health_care_service_provided like '%disclosure_support%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-linked-hiv-support-group-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children linked to HIV support group and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.health_care_service_provided like '%hiv_support_group%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-hiv-support-group-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children linked to HIV support group and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.health_care_service_provided like '%hiv_support_group%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-art-adherence-counseling-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children received ART adherence counseling and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.health_care_service_provided like '%art_adherence_counseling%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-art-adherence-counseling-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children received ART adherence counseling and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.health_care_service_provided like '%art_adherence_counseling%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-hiv-risk-assessment-M-10-14"
    grouping: "MVC"
    description: "Number of Male children received HIV risk assessment and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.health_care_service_provided like '%hiv_risk_assessment%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-hiv-risk-assessment-F-10-14"
    grouping: "MVC"
    description: "Number of Female children received HIV risk assessment and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.health_care_service_provided like '%hiv_risk_assessment%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-hiv-prevention-education-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children provided with HIV prevention education and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.health_care_service_provided like '%hiv_prevention_education%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-hiv-prevention-education-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children provided with HIV prevention education and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.health_care_service_provided like '%hiv_prevention_education%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-supported-birth-certificates-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children supported to get birth certificates and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.child_protection_service like '%supported_to_get_birth_certificates%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-supported-birth-certificates-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children supported to get birth certificates and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.child_protection_service like '%supported_to_get_birth_certificates%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-emergency-care-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children supported to get emergency care and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.child_protection_service like '%provided_with_emergency_care_and_support_services%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-emergency-care-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children supported to get emergency care and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.child_protection_service like '%provided_with_emergency_care_and_support_services%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-linked-social-welfare-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children linked with social welfare and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.child_protection_service like '%linked_with_social_welfare_office%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-social-welfare-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children linked with social welfare and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.child_protection_service like '%linked_with_social_welfare_office%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-linked-mvcc-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children linked with MVCC/Child protection team and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.child_protection_service like '%linked_with_mvcc_child_protection_team%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-mvcc-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children linked with MVCC/Child protection team and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.child_protection_service like '%linked_with_mvcc_child_protection_team%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-protection-awareness-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children provided with general child protection awareness creation and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.child_protection_service like '%provided_with_general_child_protection_awareness_creation%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-child-protection-awareness-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children provided with general child protection awareness creation and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.child_protection_service like '%provided_with_general_child_protection_awareness_creation%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-health-services-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on Health Related Services and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_provided like '%health_related_services_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-health-services-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on Health Related Services and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_provided like '%health_related_services_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-health-services-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on nutrition Services and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-health-services-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on nutrition services and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"



  #Male
  - key: "mvc-referrals-nutrition-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on nutrition Services and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-nutrition-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on nutrition services and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-education-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on nutrition Services and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_provided like '%education_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-education-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on nutrition services and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_provided like '%education_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-child-protection-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on child protection Services and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_provided like '%child_protection_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-child-protection-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on child protection services and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_provided like '%child_protection_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-psychosocial-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on psychosocial support Services and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_provided like '%psychosocial_support_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-psychosocial-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on psychosocial support services and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_provided like '%psychosocial_support_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-economic-strengthening-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on economic strengthening and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_provided like '%economic_strengthening_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-economic-strengthening-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on economic strengthening and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_provided like '%economic_strengthening_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-others-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children provided other referrals are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_provided like '%other%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-others-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children provided other referrals and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_provided like '%other%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-completed-health-services-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on health services are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_completed_by_client like '%health_related_services_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-health-services-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on health services are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_completed_by_client like '%health_related_services_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-completed-nutrition-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on health services are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_completed_by_client like '%nutrition_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-nutrition-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on health services are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_completed_by_client like '%nutrition_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-completed-education-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on education services are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_completed_by_client like '%education_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-education-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on education services are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_completed_by_client like '%education_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-completed-child-protection-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on child protection services are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_completed_by_client like '%child_protection_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-child-protection-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on child protection services are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_completed_by_client like '%child_protection_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-completed-psychosocial-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on psychosocial support services are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_completed_by_client like '%psychosocial_support_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-psychosocial-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on  psychosocial support services are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_completed_by_client like '%psychosocial_support_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-referrals-completed-economic-strengthening-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on economic strengthening services are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_completed_by_client like '%economic_strengthening_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-economic-strengthening-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on economic strengthening are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_completed_by_client like '%economic_strengthening_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-completed-others-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children who completed other referrals and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_completed_by_client like '%other%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-others-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children who completed other referrals and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.referrals_completed_by_client like '%other%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referral-accompanied-to-services-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children who completed other referrals and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.support_provided_on_referrals like '%accompanied_to_service%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referral-accompanied-to-services-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children who completed other referrals and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.support_provided_on_referrals like '%accompanied_to_service%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referral-assisted-with-transport-M-10-14"
    grouping: "MVC"
    description: "Number of Male Children Referrals assisted with transport and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.support_provided_on_referrals like '%assisted_with_transport%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referral-assisted-with-transport-F-10-14"
    grouping: "MVC"
    description: "Number of Female Children Referrals assisted with transport and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.support_provided_on_referrals like '%assisted_with_transport%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-active-MVC-M-10-14"
    grouping: "MVC"
    description: "Number of Male Active MVC who are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.visit_type = '%active_continuing_with_services%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-active-MVC-F-10-14"
    grouping: "MVC"
    description: "Number of Female Active MVC who are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.visit_type = '%active_continuing_with_services%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-graduated-MVC-M-10-14"
    grouping: "MVC"
    description: "Number of Male graduated MVC who are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.visit_type = '%graduated%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-graduated-MVC-F-10-14"
    grouping: "MVC"
    description: "Number of Female graduated MVC who are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.visit_type = '%graduated%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-transferred-MVC-M-10-14"
    grouping: "MVC"
    description: "Number of Male graduated MVC who are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.visit_type = '%transferred%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-transferred-MVC-F-10-14"
    grouping: "MVC"
    description: "Number of Female graduated MVC who are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.visit_type = '%transferred%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-exited-without-graduation-M-10-14"
    grouping: "MVC"
    description: "Number of Male who exited MVC without graduation and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.visit_type = '%exited_without_graduation%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-exited-without-graduation-F-10-14"
    grouping: "MVC"
    description: "Number of Female who exited MVC without graduation and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.visit_type = '%exited_without_graduation%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-died-MVC-M-10-14"
    grouping: "MVC"
    description: "Number of Male MVC who died and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.visit_type = '%died%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-died-MVC-F-10-14"
    grouping: "MVC"
    description: "Number of Female MVC who died and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 10 AND (date('Now') - date(efm.dob)) <= 14
                      AND ecs.visit_type = '%died%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


######################################################Indicators for aged 15 to 17 #####################################################################################
  #Male
  - key: "mvc-number-of-children-enrolled-in-school-M-15-17"
    grouping: "MVC"
    description: "Number male children registered in school aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.enrollment_date / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-number-of-children-enrolled-in-school-F-15-17"
    grouping: "MVC"
    description: "Number Female children registered in school aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.enrollment_date / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-missed-school-M-15-17"
    grouping: "MVC"
    description: "Number of Male children who missed school for three or more days consecutively in a month aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.education_and_vocational_training = 'missed_school_three_days'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-missed-school-F-15-17"
    grouping: "MVC"
    description: "Number of Female children who missed school for three or more days consecutively in a month aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.education_and_vocational_training = 'missed_school_three_days'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-linked-to-education-support-F-15-17"
    grouping: "MVC"
    description: "Number of Male children who are linked to education and support and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.education_vocational_training like '%linked_to_education_support%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-linked-to-education-support-M-15-17"
    grouping: "MVC"
    description: "Number of Female children who are linked to education and support and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.education_vocational_training like '%linked_to_education_support%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-supported-vocational-training-M-15-17"
    grouping: "MVC"
    description: "Number of Male children who are linked to vocational training support and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.ecd_psychosocial_support like '%supported_attend_vocational_training%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-supported-vocational-training-F-15-17"
    grouping: "MVC"
    description: "Number of Female children who are linked to vocational trining support and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.ecd_psychosocial_support like '%supported_attend_vocational_training%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"



  #Male
  - key: "mvc-received-business-kit-M-15-17"
    grouping: "MVC"
    description: "Number of Male children who received business kit and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.ecd_psychosocial_support like '%received_business_start_up_kit%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-received-business-kit-F-15-17"
    grouping: "MVC"
    description: "Number of Female children who received business kit and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.ecd_psychosocial_support like '%received_business_start_up_kit%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-linked-to-memkwa-M-15-17"
    grouping: "MVC"
    description: "Number of Male children who received business kit and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.ecd_psychosocial_support like '%linked_mainstreamed_education_program%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-to-memkwa-F-15-17"
    grouping: "MVC"
    description: "Number of Female children who received business kit and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.ecd_psychosocial_support like '%linked_mainstreamed_education_program%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  ###############################################Education and vocational training ends here ###########################################################################

  ###############################################ECD & Psychosocial Support starts here ###########################################################################

  #Male
  - key: "mvc-attended-ecd-M-15-17"
    grouping: "MVC"
    description: "Number of Male children who attended ECD and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.ecd_psychosocial_support like '%attended_ecd_centre%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-attended-ecd-F-15-17"
    grouping: "MVC"
    description: "Number of Female children who received business kit and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.ecd_psychosocial_support like '%attended_ecd_centre%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-attended-childrens-clubs-M-15-17"
    grouping: "MVC"
    description: "Number of Male children attended children's clubs and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.ecd_psychosocial_support like '%children_attended_childrens_clubs%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-attended-childrens-clubs-F-15-17"
    grouping: "MVC"
    description: "Number of Female children attended children's clubs and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.ecd_psychosocial_support like '%children_attended_childrens_clubs%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-attended-teen-clubs-M-15-17"
    grouping: "MVC"
    description: "Number of Male children attended teen's clubs and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.ecd_psychosocial_support like '%children_attended_teen_clubs%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-attended-teen-clubs-F-15-17"
    grouping: "MVC"
    description: "Number of Female children attended teen's clubs and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.ecd_psychosocial_support like '%children_attended_teen_clubs%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  ############################################### MVC<5 years Vaccination  ###########################################################################

  #Male
  - key: "mvc-vaccinated-M-15-17"
    grouping: "MVC"
    description: "Number of Male MVC < 5 years received required Vaccination and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.child_received_vaccinations = 'yes'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-vaccinated-F-15-17"
    grouping: "MVC"
    description: "Number of Female MVC < 5 years received required Vaccination and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.child_received_vaccinations = 'yes'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-not-vaccinated-M-15-17"
    grouping: "MVC"
    description: "Number of Male MVC < 5 years who did not receive required Vaccination and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.child_received_vaccinations = 'no'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-not-vaccinated-F-15-17"
    grouping: "MVC"
    description: "Number of Female MVC < 5 years who did not receive required Vaccination and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.child_received_vaccinations = 'no'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  ############################################### Nutritional Status  ###########################################################################

  #Male
  - key: "mvc-severe-malnutrition-M-15-17"
    grouping: "MVC"
    description: "Number of Male MVC with Severe Malnutrition and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.child_nutritional_status = 'severe_malnutrition'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-severe-malnutrition-F-15-17"
    grouping: "MVC"
    description: "Number of Female MVC with Severe Malnutrition and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.child_nutritional_status = 'severe_malnutrition'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-moderate-malnutrition-M-15-17"
    grouping: "MVC"
    description: "Number of Male MVC with Severe Malnutrition and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.child_nutritional_status = 'moderate_malnutrition'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-moderate-malnutrition-M-15-17"
    grouping: "MVC"
    description: "Number of Female MVC with Severe Malnutrition and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.child_nutritional_status = 'moderate_malnutrition'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  ############################################### Health Care  ###########################################################################

  #Male
  - key: "mvc-joined-chf-tika-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children supported to join CHF/TIKA and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.health_care_service_provided like '%supported_to_join_chf_tika%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-joined-chf-tika-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children supported to join CHF/TIKA and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.health_care_service_provided like '%supported_to_join_chf_tika%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-hiv-prevention-counseling-M-15-17"
    grouping: "MVC"
    description: "Number of Male children received HIV prevention counseling services and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.health_care_service_provided like '%hiv_prevention_counseling_service%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-hiv-prevention-counseling-F-15-17"
    grouping: "MVC"
    description: "Number of Female children received HIV prevention counseling services and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.health_care_service_provided like '%hiv_prevention_counseling_service%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-disclosure-support-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children received disclosure support and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.health_care_service_provided like '%disclosure_support%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-disclosure-support-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children received disclosure support and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.health_care_service_provided like '%disclosure_support%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-linked-hiv-support-group-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children linked to HIV support group and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.health_care_service_provided like '%hiv_support_group%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-hiv-support-group-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children linked to HIV support group and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.health_care_service_provided like '%hiv_support_group%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-art-adherence-counseling-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children received ART adherence counseling and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.health_care_service_provided like '%art_adherence_counseling%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-art-adherence-counseling-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children received ART adherence counseling and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.health_care_service_provided like '%art_adherence_counseling%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-hiv-risk-assessment-M-15-17"
    grouping: "MVC"
    description: "Number of Male children received HIV risk assessment and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.health_care_service_provided like '%hiv_risk_assessment%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-hiv-risk-assessment-F-15-17"
    grouping: "MVC"
    description: "Number of Female children received HIV risk assessment and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.health_care_service_provided like '%hiv_risk_assessment%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-hiv-prevention-education-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children provided with HIV prevention education and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.health_care_service_provided like '%hiv_prevention_education%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-hiv-prevention-education-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children provided with HIV prevention education and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.health_care_service_provided like '%hiv_prevention_education%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-supported-birth-certificates-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children supported to get birth certificates and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.child_protection_service like '%supported_to_get_birth_certificates%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-supported-birth-certificates-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children supported to get birth certificates and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.child_protection_service like '%supported_to_get_birth_certificates%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-emergency-care-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children supported to get emergency care and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.child_protection_service like '%provided_with_emergency_care_and_support_services%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-emergency-care-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children supported to get emergency care and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.child_protection_service like '%provided_with_emergency_care_and_support_services%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-linked-social-welfare-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children linked with social welfare and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.child_protection_service like '%linked_with_social_welfare_office%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-social-welfare-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children linked with social welfare and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.child_protection_service like '%linked_with_social_welfare_office%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-linked-mvcc-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children linked with MVCC/Child protection team and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.child_protection_service like '%linked_with_mvcc_child_protection_team%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-linked-mvcc-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children linked with MVCC/Child protection team and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.child_protection_service like '%linked_with_mvcc_child_protection_team%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-protection-awareness-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children provided with general child protection awareness creation and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.child_protection_service like '%provided_with_general_child_protection_awareness_creation%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-child-protection-awareness-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children provided with general child protection awareness creation and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.child_protection_service like '%provided_with_general_child_protection_awareness_creation%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-health-services-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on Health Related Services and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_provided like '%health_related_services_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-health-services-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on Health Related Services and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_provided like '%health_related_services_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-health-services-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on nutrition Services and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-health-services-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on nutrition services and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"



  #Male
  - key: "mvc-referrals-nutrition-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on nutrition Services and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-nutrition-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on nutrition services and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_provided like '%nutrition_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-education-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on nutrition Services and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_provided like '%education_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-education-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on nutrition services and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_provided like '%education_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-child-protection-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on child protection Services and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_provided like '%child_protection_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-child-protection-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on child protection services and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_provided like '%child_protection_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-psychosocial-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on psychosocial support Services and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_provided like '%psychosocial_support_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-psychosocial-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on psychosocial support services and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_provided like '%psychosocial_support_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-economic-strengthening-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children provided referrals on economic strengthening and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_provided like '%economic_strengthening_provided%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-economic-strengthening-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children provided referrals on economic strengthening and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_provided like '%economic_strengthening_provided%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-others-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children provided other referrals are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_provided like '%other%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-others-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children provided other referrals and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_provided like '%other%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-completed-health-services-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on health services are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_completed_by_client like '%health_related_services_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-health-services-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on health services are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_completed_by_client like '%health_related_services_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-completed-nutrition-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on health services are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_completed_by_client like '%nutrition_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-nutrition-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on health services are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_completed_by_client like '%nutrition_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-completed-education-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on education services are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_completed_by_client like '%education_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-education-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on education services are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_completed_by_client like '%education_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referrals-completed-child-protection-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on child protection services are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_completed_by_client like '%child_protection_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-child-protection-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on child protection services are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_completed_by_client like '%child_protection_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-completed-psychosocial-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on psychosocial support services are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_completed_by_client like '%psychosocial_support_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-psychosocial-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on  psychosocial support services are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_completed_by_client like '%psychosocial_support_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-referrals-completed-economic-strengthening-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children who completed referrals on economic strengthening services are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_completed_by_client like '%economic_strengthening_completed%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-economic-strengthening-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children who completed referrals on economic strengthening are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_completed_by_client like '%economic_strengthening_completed%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referrals-completed-others-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children who completed other referrals and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_completed_by_client like '%other%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referrals-completed-others-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children who completed other referrals and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.referrals_completed_by_client like '%other%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


  #Male
  - key: "mvc-referral-accompanied-to-services-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children who completed other referrals and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.support_provided_on_referrals like '%accompanied_to_service%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referral-accompanied-to-services-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children who completed other referrals and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.support_provided_on_referrals like '%accompanied_to_service%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-referral-assisted-with-transport-M-15-17"
    grouping: "MVC"
    description: "Number of Male Children Referrals assisted with transport and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.support_provided_on_referrals like '%assisted_with_transport%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-referral-assisted-with-transport-F-15-17"
    grouping: "MVC"
    description: "Number of Female Children Referrals assisted with transport and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.support_provided_on_referrals like '%assisted_with_transport%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-active-MVC-M-15-17"
    grouping: "MVC"
    description: "Number of Male Active MVC who are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.visit_type = '%active_continuing_with_services%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-active-MVC-F-15-17"
    grouping: "MVC"
    description: "Number of Female Active MVC who are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.visit_type = '%active_continuing_with_services%'
                      AND (efm.gender='Female')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-graduated-MVC-M-15-17"
    grouping: "MVC"
    description: "Number of Male graduated MVC who are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.visit_type = '%graduated%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-graduated-MVC-F-15-17"
    grouping: "MVC"
    description: "Number of Female graduated MVC who are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.visit_type = '%graduated%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-transferred-MVC-M-15-17"
    grouping: "MVC"
    description: "Number of Male graduated MVC who are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.visit_type = '%transferred%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-transferred-MVC-F-15-17"
    grouping: "MVC"
    description: "Number of Female graduated MVC who are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.visit_type = '%transferred%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-exited-without-graduation-M-15-17"
    grouping: "MVC"
    description: "Number of Male who exited MVC without graduation and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.visit_type = '%exited_without_graduation%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-exited-without-graduation-F-15-17"
    grouping: "MVC"
    description: "Number of Female who exited MVC without graduation and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.visit_type = '%exited_without_graduation%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-died-MVC-M-15-17"
    grouping: "MVC"
    description: "Number of Male MVC who died and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.visit_type = '%died%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-died-MVC-F-15-17"
    grouping: "MVC"
    description: "Number of Female MVC who died and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT ecs.entity_id) as count
                      FROM ec_mvc_child_services ecs
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecs.entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) <18
                      AND ecs.visit_type = '%died%'
                      AND (efm.gender='Male')
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecs.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"