#Indicators for less than 1
indicators:
  #Male
  - key: "mvc-number-registered-M-<1"
    grouping: "MVC"
    description: "Number male registered OVC children under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND (efm.gender='Male')
                      AND eor.client_type = ''
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-number-registered-F-<1"
    grouping: "MVC"
    description: "Number female registered OVC children under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND (efm.gender='Female')
                      AND eor.client_type = ''
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-birth-certificate-M-<1"
    grouping: "MVC"
    description: "Number male registered OVC children under 1 who  don't have a birth certificate"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.mvc_has_birth_certificate = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-birth-certificate-F-<1"
    grouping: "MVC"
    description: "Number female registered OVC children under 1 who have a birth certificate"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.mvc_has_birth_certificate = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-disabled-M-<1"
    grouping: "MVC"
    description: "Number male registered OVC disabled children under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.mvc_has_birth_certificate = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-disabled-F-<1"
    grouping: "MVC"
    description: "Number female registered OVC disabled children under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.mvc_has_birth_certificate = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-orphaned-hiv-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC who are orphaned due to hiv and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.reasons_of_vulnerability like '%orphan_due_to_hiv%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-orphaned-hiv-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC who are orphaned due to hiv and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.reasons_of_vulnerability like '%orphan_due_to_hiv%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-orphaned-other-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC who are orphaned due to other causes and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.reasons_of_vulnerability like '%orphaned_due_to_other_causes%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-orphaned-other-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC who are orphaned due to other causes and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.reasons_of_vulnerability like '%orphaned_due_to_other_causes%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-living-hiv-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC who are living with HIV and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.reasons_of_vulnerability like '%living_with_hiv%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-living-hiv-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC who are living with HIV and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.reasons_of_vulnerability like '%living_with_hiv%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-malnourished-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC who are living with HIV and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.reasons_of_vulnerability like '%malnourished%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-malnourished-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC who are living with HIV and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.reasons_of_vulnerability like '%malnourished%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-conflict-law-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC who are in conflict with the law and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.reasons_of_vulnerability like '%in_conflict_with_law%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-conflict-law-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC who are in conflict with the law and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.reasons_of_vulnerability like '%in_conflict_with_law%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-street-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC who are living of working on the street and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.reasons_of_vulnerability like '%living_or_working_on_the_street%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-street-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC who living of working on the street and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.reasons_of_vulnerability like '%living_or_working_on_the_street%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-institutional-care-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC who are living in institutional care and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.reasons_of_vulnerability like '%living_in_institutional_care%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-institutional-care-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC who living in institutional care and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.reasons_of_vulnerability like '%living_in_institutional_care%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-worst-labor-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC who  in worst form of child labor are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.reasons_of_vulnerability like '%worst_form_of_child_labor%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-worst-labor-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC who are in worst form of child labor are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.reasons_of_vulnerability like '%worst_form_of_child_labor%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-violence-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC who are at risk or suffering from violence, abuse and neglect are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.reasons_of_vulnerability like '%children_at_risk_or_suffering_from_violence_abuse_and_neglect%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-violence-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC who have other vulnerabilities and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.reasons_of_vulnerability like '%children_at_risk_or_suffering_from_violence_abuse_and_neglect%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-others-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC who have other vulnerabilities and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.reasons_of_vulnerability like '%other%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-others-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC who living in institutional care and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.reasons_of_vulnerability like '%other%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-albinism-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is albinism and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.type_of_disability like '%albinism%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-albinism-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is albinism and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.type_of_disability like '%albinism%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-mental-disability-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is mental disability and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.type_of_disability like '%mental_disability%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-mental-disability-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is mental disability and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.type_of_disability like '%mental_disability%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-hearing-disability-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is hearing disability and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.type_of_disability like '%deaf%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-hearing-disability-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is hearing disability and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.type_of_disability like '%deaf%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-visual-disability-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is hearing disability and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.type_of_disability like '%blind%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-visual-disability-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is hearing disability and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.type_of_disability like '%blind%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-physical-disability-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is physical disability and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.type_of_disability like '%physical_disability%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-physical-disability-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is physical disability and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.type_of_disability like '%physical_disability%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-speech-impairment-1-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is speech impairment and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.type_of_disability like '%impairment_of_speech%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-speech-impairment-1-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is speech impairment and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.type_of_disability like '%impairment_of_speech%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-others-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is other and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.type_of_disability like '%other%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-others-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is other and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.type_of_disability like '%other%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-in-school-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-in-school-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-not-in-school-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC who are not in school and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND eor.is_child_in_school = 'no'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-not-in-school-F<1"
    grouping: "MVC"
    description: "Number Female registered OVC who are not in school and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
                      AND eor.is_child_in_school = 'no'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-early-childhood-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in ecd level and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'ecd'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-early-childhood-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in ecd level and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'ecd'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-pre-primary-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in pre-primary level and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'pre_primary_school'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-pre-primary-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in pre-primary level and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'pre_primary_school'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-primary-school-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in primary school level and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'primary_school'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-primary-school-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in primary school level and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'primary_school'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-secondary-school-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in secondary school level and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'secondary_school'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-secondary-school-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in secondary school level and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'secondary_school'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-vocational-training-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in vocational trainings level and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'vocational_trainings'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-vocational-training-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in vocational trainings level and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'vocational_trainings'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-college-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in colleges level and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'colleges'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-college-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in colleges level and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'colleges'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-father-mother-care-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is father/mother and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'father'
					  OR eor.care_of_household = 'mother'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-father-mother-care-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is father/mother and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'father'
					  OR eor.care_of_household = 'mother'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-grandparent-care-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is grand father/grand mother and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'grandfather'
					  OR eor.care_of_household = 'grandmother'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-grandparent-care-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is grandfather/grandmother and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'grandfather'
					  OR eor.care_of_household = 'grandmother'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-aunt-uncle-care-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is aunt/uncle and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'aunt'
					  OR eor.care_of_household = 'uncle'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-aunt-uncle-care-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is aunt/uncle and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'aunt'
					  OR eor.care_of_household = 'uncle'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-step-parent-care-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is stepmother/stepfather and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'stepfather'
					  OR eor.care_of_household = 'stepmother'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-step-parent-care-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is stepfather/stepmother and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'stepfather'
					  OR eor.care_of_household = 'stepmother'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-sibling-care-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is brother/sister and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'sister'
					  OR eor.care_of_household = 'brother'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-sibling-care-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is brother/sister and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'sister'
					  OR eor.care_of_household = 'brother'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-institutional-care-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is brother/sister and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'institutional'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-institutional-care-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is brother/sister and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'institutional'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-cousin-care-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is cousin and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'cousin'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-cousin-care-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is cousin and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'cousin'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-family-friend-care-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is cousin and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'family_friend'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-family-friend-care-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is cousin and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'family_friend'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-foster-parent-care-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is foster parent and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'foster_parent'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-foster-parent-care-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is foster parent and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'foster_parent'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-adoptive-parent-care-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is adoptive parent and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'adoptive_parent'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-adoptive-parent-care-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is adoptive parent and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'adoptive_parent'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-other-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is other and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'other'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-child-primary-caregiver-other-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is other and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.care_of_household = 'other'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-number-cases-opened-M-<1"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is other and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.case_opened = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-child-primary-caregiver-number-cases-opened-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is other and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.case_opened = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-cases-opened-M-<1"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is other and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) < 1
					  AND eor.case_opened = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-cases-opened-F-<1"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is other and are under 1"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.case_opened = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

#Indicators for less for age between 1 and 5
  #Male
  - key: "mvc-number-registered-M-1-4"
    grouping: "MVC"
    description: "Number male registered OVC children of age between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND (efm.gender='Male')
                      AND eor.client_type = ''
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-number-registered-F-1-4"
    grouping: "MVC"
    description: "Number female registered OVC children aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND (efm.gender='Female')
                      AND eor.client_type = ''
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-birth-certificate-M-1-4"
    grouping: "MVC"
    description: "Number male registered OVC children aged between 1 and 5 who don't have a birth certificate"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.mvc_has_birth_certificate = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-birth-certificate-F-1-4"
    grouping: "MVC"
    description: "Number female registered OVC children aged between 1 and 5 and have a birth certificate"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.mvc_has_birth_certificate = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-disabled-M-1-4"
    grouping: "MVC"
    description: "Number male registered OVC disabled children aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.mvc_has_birth_certificate = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-disabled-F-1-4"
    grouping: "MVC"
    description: "Number female registered OVC disabled children aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.mvc_has_birth_certificate = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-orphaned-hiv-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC who are orphaned due to hiv aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.reasons_of_vulnerability like '%orphan_due_to_hiv%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-orphaned-hiv-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC who are orphaned due to hiv and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.reasons_of_vulnerability like '%orphan_due_to_hiv%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-orphaned-other-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC who are orphaned due to other causes and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.reasons_of_vulnerability like '%orphaned_due_to_other_causes%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-orphaned-other-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC who are orphaned due to other causes and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.reasons_of_vulnerability like '%orphaned_due_to_other_causes%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-living-hiv-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC who are living with HIV and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.reasons_of_vulnerability like '%living_with_hiv%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-living-hiv-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC who are living with HIV and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.reasons_of_vulnerability like '%living_with_hiv%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-malnourished-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC who are living with HIV and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.reasons_of_vulnerability like '%malnourished%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-malnourished-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC who are living with HIV and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.reasons_of_vulnerability like '%malnourished%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-conflict-law-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC who are in conflict with the law and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.reasons_of_vulnerability like '%in_conflict_with_law%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-conflict-law-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC who are in conflict with the law and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.reasons_of_vulnerability like '%in_conflict_with_law%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-street-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC who are living of working on the street and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.reasons_of_vulnerability like '%living_or_working_on_the_street%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-street-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC who living of working on the street and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.reasons_of_vulnerability like '%living_or_working_on_the_street%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-institutional-care-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC who are living in institutional care and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.reasons_of_vulnerability like '%living_in_institutional_care%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-institutional-care-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC who living in institutional care and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.reasons_of_vulnerability like '%living_in_institutional_care%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-worst-labor-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC who  in worst form of child labor are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.reasons_of_vulnerability like '%worst_form_of_child_labor%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-worst-labor-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC who are in worst form of child labor are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.reasons_of_vulnerability like '%worst_form_of_child_labor%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-violence-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC who are at risk or suffering from violence, abuse and neglect are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.reasons_of_vulnerability like '%children_at_risk_or_suffering_from_violence_abuse_and_neglect%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-violence-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC who have other vulnerabilities and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.reasons_of_vulnerability like '%children_at_risk_or_suffering_from_violence_abuse_and_neglect%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-others-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC who have other vulnerabilities and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.reasons_of_vulnerability like '%other%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-others-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC who living in institutional care and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.reasons_of_vulnerability like '%other%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-albinism-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is albinism and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.type_of_disability like '%albinism%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-albinism-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is albinism and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.type_of_disability like '%albinism%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-mental-disability-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is mental disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.type_of_disability like '%mental_disability%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-mental-disability-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is mental disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.type_of_disability like '%mental_disability%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-hearing-disability-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is hearing disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.type_of_disability like '%deaf%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-hearing-disability-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is hearing disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.type_of_disability like '%deaf%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-visual-disability-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is hearing disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.type_of_disability like '%blind%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-visual-disability-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is hearing disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.type_of_disability like '%blind%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-physical-disability-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is physical disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.type_of_disability like '%physical_disability%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-physical-disability-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is physical disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.type_of_disability like '%physical_disability%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-speech-impairment-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is speech impairment and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.type_of_disability like '%impairment_of_speech%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-speech-impairment-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is speech impairment and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.type_of_disability like '%impairment_of_speech%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-others-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is other and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.type_of_disability like '%other%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-others-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is other and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.type_of_disability like '%other%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-in-school-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-in-school-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-not-in-school-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC who are not in school and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND eor.is_child_in_school = 'no'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-not-in-school-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC who are not in school and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
                      AND eor.is_child_in_school = 'no'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-early-childhood-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in ecd level and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'ecd'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-early-childhood-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in ecd level and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'ecd'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-pre-primary-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in pre-primary level and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'pre_primary_school'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-pre-primary-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in pre-primary level and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'pre_primary_school'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-primary-school-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in primary school level and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'primary_school'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-primary-school-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in primary school level and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'primary_school'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-secondary-school-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in secondary school level and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'secondary_school'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-secondary-school-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in secondary school level and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'secondary_school'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-vocational-training-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in vocational trainings level and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'vocational_trainings'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-vocational-training-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in vocational trainings level and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'vocational_trainings'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-college-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in colleges level and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'colleges'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-college-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in colleges level and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'colleges'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-father-mother-care-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is father/mother and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'father'
					  OR eor.care_of_household = 'mother'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-father-mother-care-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is father/mother and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'father'
					  OR eor.care_of_household = 'mother'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-grandparent-care-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is grand father/grand mother and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'grandfather'
					  OR eor.care_of_household = 'grandmother'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-grandparent-care-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is grandfather/grandmother and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'grandfather'
					  OR eor.care_of_household = 'grandmother'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-aunt-uncle-care-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is aunt/uncle and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'aunt'
					  OR eor.care_of_household = 'uncle'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-aunt-uncle-care-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is aunt/uncle and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'aunt'
					  OR eor.care_of_household = 'uncle'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-step-parent-care-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is stepmother/stepfather and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'stepfather'
					  OR eor.care_of_household = 'stepmother'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-step-parent-care-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is stepfather/stepmother and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'stepfather'
					  OR eor.care_of_household = 'stepmother'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-sibling-care-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is brother/sister and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'sister'
					  OR eor.care_of_household = 'brother'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-sibling-care-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is brother/sister and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'sister'
					  OR eor.care_of_household = 'brother'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-institutional-care-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is brother/sister and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'institutional'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-institutional-care-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is brother/sister and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'institutional'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-cousin-care-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is cousin and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'cousin'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-cousin-care-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is cousin and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'cousin'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-family-friend-care-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is cousin and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'family_friend'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-family-friend-care-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is cousin and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'family_friend'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-foster-parent-care-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is foster parent and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'foster_parent'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-foster-parent-care-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is foster parent and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'foster_parent'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-adoptive-parent-care-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is adoptive parent and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'adoptive_parent'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-adoptive-parent-care-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is adoptive parent and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'adoptive_parent'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-other-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is other and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'other'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-child-primary-caregiver-other-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is other and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.care_of_household = 'other'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-number-cases-opened-M-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is other and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.case_opened = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-child-primary-caregiver-number-cases-opened-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is other and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.case_opened = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-cases-opened-M-1-4"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is other and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.case_opened = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-cases-opened-F-1-4"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is other and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 1 AND (date('Now') - date(efm.dob)) < 5
					  AND eor.case_opened = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"



#Indicators for age group 5 to 9
  #Male
  - key: "mvc-number-registered-M-5-9"
    grouping: "MVC"
    description: "Number male registered OVC children of age from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-number-registered-F-5-9"
    grouping: "MVC"
    description: "Number female registered OVC children aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-birth-certificate-M-5-9"
    grouping: "MVC"
    description: "Number male registered OVC children aged between 1 and 5 who don't have a birth certificate"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.mvc_has_birth_certificate = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-birth-certificate-F-5-9"
    grouping: "MVC"
    description: "Number female registered OVC children aged between 1 and 5 and have a birth certificate"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.mvc_has_birth_certificate = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-disabled-M-5-9"
    grouping: "MVC"
    description: "Number male registered OVC disabled children aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.mvc_has_birth_certificate = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-disabled-F-5-9"
    grouping: "MVC"
    description: "Number female registered OVC disabled children aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.mvc_has_birth_certificate = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-orphaned-hiv-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC who are orphaned due to hiv aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.reasons_of_vulnerability like '%orphan_due_to_hiv%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-orphaned-hiv-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC who are orphaned due to hiv and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.reasons_of_vulnerability like '%orphan_due_to_hiv%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-orphaned-other-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC who are orphaned due to other causes and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.reasons_of_vulnerability like '%orphaned_due_to_other_causes%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-orphaned-other-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC who are orphaned due to other causes and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.reasons_of_vulnerability like '%orphaned_due_to_other_causes%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-living-hiv-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC who are living with HIV and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.reasons_of_vulnerability like '%living_with_hiv%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-living-hiv-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC who are living with HIV and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.reasons_of_vulnerability like '%living_with_hiv%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-malnourished-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC who are living with HIV and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.reasons_of_vulnerability like '%malnourished%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-malnourished-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC who are living with HIV and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.reasons_of_vulnerability like '%malnourished%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-conflict-law-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC who are in conflict with the law and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.reasons_of_vulnerability like '%in_conflict_with_law%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-conflict-law-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC who are in conflict with the law and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.reasons_of_vulnerability like '%in_conflict_with_law%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-street-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC who are living of working on the street and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.reasons_of_vulnerability like '%living_or_working_on_the_street%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-street-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC who living of working on the street and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.reasons_of_vulnerability like '%living_or_working_on_the_street%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-institutional-care-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC who are living in institutional care and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.reasons_of_vulnerability like '%living_in_institutional_care%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-institutional-care-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC who living in institutional care and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.reasons_of_vulnerability like '%living_in_institutional_care%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-worst-labor-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC who  in worst form of child labor are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.reasons_of_vulnerability like '%worst_form_of_child_labor%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-worst-labor-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC who are in worst form of child labor are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.reasons_of_vulnerability like '%worst_form_of_child_labor%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-violence-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC who are at risk or suffering from violence, abuse and neglect are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.reasons_of_vulnerability like '%children_at_risk_or_suffering_from_violence_abuse_and_neglect%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-violence-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC who have other vulnerabilities and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.reasons_of_vulnerability like '%children_at_risk_or_suffering_from_violence_abuse_and_neglect%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-others-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC who have other vulnerabilities and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.reasons_of_vulnerability like '%other%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-others-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC who living in institutional care and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.reasons_of_vulnerability like '%other%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-albinism-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is albinism and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.type_of_disability like '%albinism%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-albinism-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is albinism and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.type_of_disability like '%albinism%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-mental-disability-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is mental disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.type_of_disability like '%mental_disability%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-mental-disability-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is mental disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.type_of_disability like '%mental_disability%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-hearing-disability-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is hearing disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.type_of_disability like '%deaf%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-hearing-disability-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is hearing disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.type_of_disability like '%deaf%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-visual-disability-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is hearing disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.type_of_disability like '%blind%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-visual-disability-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is hearing disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.type_of_disability like '%blind%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-physical-disability-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is physical disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.type_of_disability like '%physical_disability%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-physical-disability-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is physical disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.type_of_disability like '%physical_disability%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-speech-impairment-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is speech impairment and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.type_of_disability like '%impairment_of_speech%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-speech-impairment-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is speech impairment and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.type_of_disability like '%impairment_of_speech%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-others-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is other and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.type_of_disability like '%other%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-others-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is other and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.type_of_disability like '%other%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-in-school-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-in-school-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-not-in-school-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC who are not in school and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
                      AND eor.is_child_in_school = 'no'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-not-in-school-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC who are not in school and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
                      AND eor.is_child_in_school = 'no'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-early-childhood-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in ecd level and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'ecd'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-early-childhood-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in ecd level and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'ecd'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-pre-primary-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in pre-primary level and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'pre_primary_school'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-pre-primary-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in pre-primary level and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'pre_primary_school'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-primary-school-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in primary school level and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'primary_school'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-primary-school-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in primary school level and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'primary_school'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-secondary-school-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in secondary school level and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'secondary_school'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-secondary-school-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in secondary school level and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'secondary_school'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-vocational-training-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in vocational trainings level and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'vocational_trainings'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-vocational-training-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in vocational trainings level and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'vocational_trainings'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-college-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in colleges level and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'colleges'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-college-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in colleges level and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'colleges'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-father-mother-care-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is father/mother and are aged between from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'father'
					  OR eor.care_of_household = 'mother'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-father-mother-care-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is father/mother and are aged between from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'father'
					  OR eor.care_of_household = 'mother'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-grandparent-care-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is grand father/grand mother and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'grandfather'
					  OR eor.care_of_household = 'grandmother'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-grandparent-care-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is grandfather/grandmother and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'grandfather'
					  OR eor.care_of_household = 'grandmother'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-aunt-uncle-care-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is aunt/uncle and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'aunt'
					  OR eor.care_of_household = 'uncle'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-aunt-uncle-care-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is aunt/uncle and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'aunt'
					  OR eor.care_of_household = 'uncle'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-step-parent-care-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is stepmother/stepfather and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'stepfather'
					  OR eor.care_of_household = 'stepmother'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-step-parent-care-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is stepfather/stepmother and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'stepfather'
					  OR eor.care_of_household = 'stepmother'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-sibling-care-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is brother/sister and are aged between from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'sister'
					  OR eor.care_of_household = 'brother'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-sibling-care-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is brother/sister and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'sister'
					  OR eor.care_of_household = 'brother'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-institutional-care-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is brother/sister and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'institutional'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-institutional-care-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is brother/sister and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'institutional'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-cousin-care-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is cousin and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'cousin'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-cousin-care-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is cousin and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'cousin'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-family-friend-care-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is cousin and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'family_friend'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-family-friend-care-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is cousin and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'family_friend'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-foster-parent-care-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is foster parent and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'foster_parent'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-foster-parent-care-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is foster parent and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'foster_parent'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-adoptive-parent-care-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is adoptive parent and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'adoptive_parent'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-adoptive-parent-care-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is adoptive parent and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'adoptive_parent'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-other-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is other and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'other'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-child-primary-caregiver-other-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is other and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.care_of_household = 'other'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-number-cases-opened-M-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is other and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.case_opened = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-child-primary-caregiver-number-cases-opened-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is other and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.case_opened = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-cases-opened-M-5-9"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is other and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.case_opened = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-cases-opened-F-5-9"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is other and are aged from 5 to 9"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 4 AND (date('Now') - date(efm.dob)) < 10
					  AND eor.case_opened = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


###############################################################Indicators for age group 10 to 14##################################################################
  #Male
  - key: "mvc-number-registered-M-10-14"
    grouping: "MVC"
    description: "Number male registered OVC children of age from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-number-registered-F-10-14"
    grouping: "MVC"
    description: "Number female registered OVC children aged between 9 and 15"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-birth-certificate-M-10-14"
    grouping: "MVC"
    description: "Number male registered OVC children aged between 9 and 15 who don't have a birth certificate"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.mvc_has_birth_certificate = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-birth-certificate-F-10-14"
    grouping: "MVC"
    description: "Number female registered OVC children aged between 9 and 15 and have a birth certificate"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.mvc_has_birth_certificate = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-disabled-M-10-14"
    grouping: "MVC"
    description: "Number male registered OVC disabled children aged between 10 and 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.mvc_has_birth_certificate = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-disabled-F-10-14"
    grouping: "MVC"
    description: "Number female registered OVC disabled children aged between 10 and 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.mvc_has_birth_certificate = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-orphaned-hiv-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC who are orphaned due to hiv aged between 10 and 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.reasons_of_vulnerability like '%orphan_due_to_hiv%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-orphaned-hiv-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC who are orphaned due to hiv and aged between 10 and 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.reasons_of_vulnerability like '%orphan_due_to_hiv%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-orphaned-other-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC who are orphaned due to other causes and aged between 10 and 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.reasons_of_vulnerability like '%orphaned_due_to_other_causes%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-orphaned-other-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC who are orphaned due to other causes and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.reasons_of_vulnerability like '%orphaned_due_to_other_causes%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-living-hiv-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC who are living with HIV and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.reasons_of_vulnerability like '%living_with_hiv%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-living-hiv-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC who are living with HIV and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.reasons_of_vulnerability like '%living_with_hiv%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-malnourished-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC who are living with HIV and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.reasons_of_vulnerability like '%malnourished%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-malnourished-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC who are living with HIV and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.reasons_of_vulnerability like '%malnourished%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-conflict-law-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC who are in conflict with the law and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.reasons_of_vulnerability like '%in_conflict_with_law%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-conflict-law-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC who are in conflict with the law and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.reasons_of_vulnerability like '%in_conflict_with_law%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-street-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC who are living of working on the street and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.reasons_of_vulnerability like '%living_or_working_on_the_street%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-street-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC who living of working on the street and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.reasons_of_vulnerability like '%living_or_working_on_the_street%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-institutional-care-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC who are living in institutional care and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.reasons_of_vulnerability like '%living_in_institutional_care%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-institutional-care-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC who living in institutional care and aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.reasons_of_vulnerability like '%living_in_institutional_care%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-worst-labor-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC who  in worst form of child labor are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.reasons_of_vulnerability like '%worst_form_of_child_labor%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-worst-labor-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC who are in worst form of child labor are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.reasons_of_vulnerability like '%worst_form_of_child_labor%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-violence-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC who are at risk or suffering from violence, abuse and neglect are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.reasons_of_vulnerability like '%children_at_risk_or_suffering_from_violence_abuse_and_neglect%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-violence-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC who have other vulnerabilities and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.reasons_of_vulnerability like '%children_at_risk_or_suffering_from_violence_abuse_and_neglect%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-others-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC who have other vulnerabilities and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.reasons_of_vulnerability like '%other%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-others-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC who living in institutional care and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.reasons_of_vulnerability like '%other%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-albinism-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is albinism and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.type_of_disability like '%albinism%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-albinism-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is albinism and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.type_of_disability like '%albinism%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-mental-disability-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is mental disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.type_of_disability like '%mental_disability%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-mental-disability-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is mental disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.type_of_disability like '%mental_disability%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-hearing-disability-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is hearing disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.type_of_disability like '%deaf%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-hearing-disability-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is hearing disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.type_of_disability like '%deaf%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-visual-disability-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is hearing disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.type_of_disability like '%blind%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-visual-disability-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is hearing disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.type_of_disability like '%blind%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-physical-disability-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is physical disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.type_of_disability like '%physical_disability%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-physical-disability-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is physical disability and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.type_of_disability like '%physical_disability%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-speech-impairment-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is speech impairment and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.type_of_disability like '%impairment_of_speech%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-speech-impairment-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is speech impairment and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.type_of_disability like '%impairment_of_speech%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-others-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is other and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.type_of_disability like '%other%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-others-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is other and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.type_of_disability like '%other%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-in-school-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-in-school-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-not-in-school-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC who are not in school and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
                      AND eor.is_child_in_school = 'no'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-not-in-school-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC who are not in school and are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
                      AND eor.is_child_in_school = 'no'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-early-childhood-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in ecd level and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'ecd'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-early-childhood-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in ecd level and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'ecd'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-pre-primary-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in pre-primary level and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'pre_primary_school'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-pre-primary-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in pre-primary level and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'pre_primary_school'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-primary-school-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in primary school level and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'primary_school'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-primary-school-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in primary school level and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'primary_school'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-secondary-school-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in secondary school level and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'secondary_school'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-secondary-school-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in secondary school level and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'secondary_school'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-vocational-training-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in vocational trainings level and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'vocational_trainings'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-vocational-training-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in vocational trainings level and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'vocational_trainings'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-college-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in colleges level and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'colleges'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-college-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in colleges level and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'colleges'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-father-mother-care-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is father/mother and are aged between from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'father'
					  OR eor.care_of_household = 'mother'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-father-mother-care-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is father/mother and are aged between from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'father'
					  OR eor.care_of_household = 'mother'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-grandparent-care-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is grand father/grand mother and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'grandfather'
					  OR eor.care_of_household = 'grandmother'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-grandparent-care-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is grandfather/grandmother and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'grandfather'
					  OR eor.care_of_household = 'grandmother'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-aunt-uncle-care-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is aunt/uncle and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'aunt'
					  OR eor.care_of_household = 'uncle'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-aunt-uncle-care-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is aunt/uncle and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'aunt'
					  OR eor.care_of_household = 'uncle'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-step-parent-care-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is stepmother/stepfather and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'stepfather'
					  OR eor.care_of_household = 'stepmother'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-step-parent-care-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is stepfather/stepmother and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'stepfather'
					  OR eor.care_of_household = 'stepmother'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-sibling-care-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is brother/sister and are aged between from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'sister'
					  OR eor.care_of_household = 'brother'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-sibling-care-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is brother/sister and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'sister'
					  OR eor.care_of_household = 'brother'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-institutional-care-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is brother/sister and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'institutional'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-institutional-care-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is brother/sister and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'institutional'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-cousin-care-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is cousin and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'cousin'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-cousin-care-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is cousin and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'cousin'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-family-friend-care-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is cousin and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'family_friend'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-family-friend-care-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is cousin and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'family_friend'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-foster-parent-care-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is foster parent and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'foster_parent'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-foster-parent-care-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is foster parent and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'foster_parent'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-adoptive-parent-care-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is adoptive parent and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'adoptive_parent'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-adoptive-parent-care-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is adoptive parent and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'adoptive_parent'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-other-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is other and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'other'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-child-primary-caregiver-other-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is other and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.care_of_household = 'other'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-number-cases-opened-M-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is other and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.case_opened = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-child-primary-caregiver-number-cases-opened-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is other and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.case_opened = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-cases-opened-M-10-14"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is other and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.case_opened = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-cases-opened-F-10-14"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is other and are aged from 10 to 14"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) > 9 AND (date('Now') - date(efm.dob)) < 15
					  AND eor.case_opened = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


############################################################# Indicators for age group 15 to 17 ##################################################################
  #Male
  - key: "mvc-number-registered-M-15-17"
    grouping: "MVC"
    description: "Number male registered OVC children of age from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-number-registered-F-15-17"
    grouping: "MVC"
    description: "Number female registered OVC children aged between from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-birth-certificate-M-15-17"
    grouping: "MVC"
    description: "Number male registered OVC children aged between from 15 to 17 who don't have a birth certificate"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.mvc_has_birth_certificate = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-birth-certificate-F-15-17"
    grouping: "MVC"
    description: "Number female registered OVC children aged between from 15 to 17 and have a birth certificate"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.mvc_has_birth_certificate = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-disabled-M-15-17"
    grouping: "MVC"
    description: "Number male registered OVC disabled children aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.mvc_has_birth_certificate = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-disabled-F-15-17"
    grouping: "MVC"
    description: "Number female registered OVC disabled children aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.mvc_has_birth_certificate = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-orphaned-hiv-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC who are orphaned due to hiv aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.reasons_of_vulnerability like '%orphan_due_to_hiv%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-orphaned-hiv-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC who are orphaned due to hiv and aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.reasons_of_vulnerability like '%orphan_due_to_hiv%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-orphaned-other-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC who are orphaned due to other causes and aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.reasons_of_vulnerability like '%orphaned_due_to_other_causes%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-orphaned-other-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC who are orphaned due to other causes and aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.reasons_of_vulnerability like '%orphaned_due_to_other_causes%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-living-hiv-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC who are living with HIV and aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.reasons_of_vulnerability like '%living_with_hiv%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-living-hiv-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC who are living with HIV and aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.reasons_of_vulnerability like '%living_with_hiv%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-malnourished-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC who are living with HIV and aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.reasons_of_vulnerability like '%malnourished%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-malnourished-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC who are living with HIV and aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.reasons_of_vulnerability like '%malnourished%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-conflict-law-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC who are in conflict with the law and aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.reasons_of_vulnerability like '%in_conflict_with_law%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-conflict-law-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC who are in conflict with the law and aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.reasons_of_vulnerability like '%in_conflict_with_law%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-type-of-vulnerability-street-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC who are living of working on the street and aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.reasons_of_vulnerability like '%living_or_working_on_the_street%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-street-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC who living of working on the street and aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.reasons_of_vulnerability like '%living_or_working_on_the_street%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-institutional-care-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC who are living in institutional care and aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.reasons_of_vulnerability like '%living_in_institutional_care%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-institutional-care-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC who living in institutional care and aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.reasons_of_vulnerability like '%living_in_institutional_care%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-worst-labor-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC who  in worst form of child labor are aged between 1 and 5"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.reasons_of_vulnerability like '%worst_form_of_child_labor%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-worst-labor-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC who are in worst form of child labor are aged between from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.reasons_of_vulnerability like '%worst_form_of_child_labor%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-violence-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC who are at risk or suffering from violence, abuse and neglect are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.reasons_of_vulnerability like '%children_at_risk_or_suffering_from_violence_abuse_and_neglect%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-violence-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC who have other vulnerabilities and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.reasons_of_vulnerability like '%children_at_risk_or_suffering_from_violence_abuse_and_neglect%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-vulnerability-others-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC who have other vulnerabilities and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.reasons_of_vulnerability like '%other%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-vulnerability-others-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC who living in institutional care and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.reasons_of_vulnerability like '%other%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-albinism-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is albinism and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.type_of_disability like '%albinism%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-albinism-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is albinism and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.type_of_disability like '%albinism%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-mental-disability-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is mental disability and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.type_of_disability like '%mental_disability%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-mental-disability-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is mental disability and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.type_of_disability like '%mental_disability%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-hearing-disability-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is hearing disability and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.type_of_disability like '%deaf%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-hearing-disability-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is hearing disability and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.type_of_disability like '%deaf%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-visual-disability-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is hearing disability and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.type_of_disability like '%blind%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-visual-disability-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is hearing disability and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.type_of_disability like '%blind%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-physical-disability-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is physical disability and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.type_of_disability like '%physical_disability%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-physical-disability-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is physical disability and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.type_of_disability like '%physical_disability%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-speech-impairment-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is speech impairment and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.type_of_disability like '%impairment_of_speech%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-speech-impairment-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is speech impairment and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.type_of_disability like '%impairment_of_speech%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-type-of-disability-others-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC whose type of disability is other and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.type_of_disability like '%other%'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-type-of-disability-others-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC whose type of disability is other and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.type_of_disability like '%other%'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-in-school-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and are aged between from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-in-school-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
                      AND eor.is_child_in_school = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-not-in-school-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC who are not in school and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
                      AND eor.is_child_in_school = 'no'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-not-in-school-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC who are not in school and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
                      AND eor.is_child_in_school = 'no'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-early-childhood-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in ecd level and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'ecd'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-early-childhood-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in ecd level and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'ecd'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-pre-primary-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in pre-primary level and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'pre_primary_school'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-pre-primary-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in pre-primary level and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'pre_primary_school'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-primary-school-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in primary school level and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'primary_school'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-primary-school-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in primary school level and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'primary_school'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-secondary-school-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in secondary school level and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'secondary_school'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-secondary-school-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in secondary school level and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'secondary_school'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-vocational-training-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in vocational trainings level and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'vocational_trainings'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-vocational-training-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in vocational trainings level and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'vocational_trainings'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-level-of-education-college-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC who are in school and in colleges level and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'colleges'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-level-of-education-college-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC who are in school and in colleges level and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.is_child_in_school = 'yes'
					  AND eor.level_of_education = 'colleges'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-father-mother-care-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is father/mother and are aged between from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'father'
					  OR eor.care_of_household = 'mother'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-father-mother-care-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is father/mother and are aged between from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'father'
					  OR eor.care_of_household = 'mother'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-grandparent-care-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is grand father/grand mother and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'grandfather'
					  OR eor.care_of_household = 'grandmother'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-grandparent-care-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is grandfather/grandmother and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'grandfather'
					  OR eor.care_of_household = 'grandmother'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-aunt-uncle-care-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is aunt/uncle and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'aunt'
					  OR eor.care_of_household = 'uncle'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-aunt-uncle-care-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is aunt/uncle and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'aunt'
					  OR eor.care_of_household = 'uncle'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-step-parent-care-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is stepmother/stepfather and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'stepfather'
					  OR eor.care_of_household = 'stepmother'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-step-parent-care-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is stepfather/stepmother and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'stepfather'
					  OR eor.care_of_household = 'stepmother'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-sibling-care-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is brother/sister and are aged between from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'sister'
					  OR eor.care_of_household = 'brother'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-sibling-care-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is brother/sister and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'sister'
					  OR eor.care_of_household = 'brother'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-institutional-care-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is brother/sister and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'institutional'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-institutional-care-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is brother/sister and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'institutional'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-cousin-care-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is cousin and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'cousin'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-cousin-care-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is cousin and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'cousin'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-family-friend-care-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is cousin and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'family_friend'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-family-friend-care-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is cousin and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'family_friend'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-foster-parent-care-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is foster parent and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'foster_parent'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-foster-parent-care-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is foster parent and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'foster_parent'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-adoptive-parent-care-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is adoptive parent and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'adoptive_parent'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-child-primary-caregiver-adoptive-parent-care-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is adoptive parent and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'adoptive_parent'
					  AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Male
  - key: "mvc-child-primary-caregiver-other-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is other and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'other'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-child-primary-caregiver-other-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is other and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.care_of_household = 'other'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-child-primary-caregiver-number-cases-opened-M-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is other and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.case_opened = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Female
  - key: "mvc-child-primary-caregiver-number-cases-opened-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is other and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.case_opened = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"

  #Male
  - key: "mvc-cases-opened-M-15-17"
    grouping: "MVC"
    description: "Number Male registered OVC whose primary care giver is other and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.case_opened = 'yes'
                      AND (efm.gender='Male')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"
  #Female
  - key: "mvc-cases-opened-F-15-17"
    grouping: "MVC"
    description: "Number Female registered OVC whose primary care giver is other and are aged from 15 to 17"
    indicatorQuery: " SELECT count(DISTINCT eor.base_entity_id) as count
                      FROM ec_ovc_register eor
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = eor.base_entity_id
                      WHERE  (date('Now') - date(efm.dob)) >= 15 AND (date('Now') - date(efm.dob)) < 18
					  AND eor.case_opened = 'yes'
                      AND (efm.gender='Female')
                      AND eor.client_type = 'ovc_child'
                      AND date((substr('%s', 1, 7) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(eor.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 7) || '-' || '01')"


